# 多租户权限隔离审计 — 执行工单模板

> 打印或复制此模板到审计任务管理系统（JIRA/Notion/飞书）。逐项检查并记录证据。

---

## 基本信息

| 项 | 值 |
|---|---|
| **项目名** | 有证慧催 |
| **审计版本** | v{version_number} |
| **审计日期** | ______年 ______月 ______日 |
| **审计人** | ______________________ |
| **审批人** | ______________________ |
| **审计环节** | ☐ 代码审查  ☐ 功能测试  ☐ 上线前  ☐ 月度定期 |

---

## 检查项详表

### 🔴 HIGH优先级（7项）— 必须通过

#### 1️⃣ 菜单隐藏 = API权限校验 

**检查状态**：☐ PASS  ☐ FAIL  ☐ PARTIAL  ☐ N/A

**检查方法**：
- [ ] 选一个受限角色(如CO)，列举其导航菜单中不应出现的项
  ```
  禁用菜单项：_________________________________
  ```
  
- [ ] 尝试直链访问对应URL，观察是否被拒或重定向
  ```
  测试URL: ___________________________________
  预期结果: 403 / 重定向到首页
  实际结果: ___________________________________
  ```

- [ ] grep后端源码确认权限装饰器覆盖
  ```bash
  grep "@Permission\|@Perm" backend/src/controller/ | wc -l
  结果: ________ 处权限装饰器
  ```

- [ ] 选一个受限API，用curl绕过前端调用
  ```bash
  curl -X POST http://localhost:8080/api/...  -H "Authorization: Bearer {token}"
  预期: 403 Forbidden
  实际: ___________________________________
  ```

**证据附件**：
- 后端@Perm装饰器截图（行号：__________）
- curl请求与响应日志
- 权限校验代码片段

**结论**：
- [ ] 所有导航隐藏都有后端权限校验
- [ ] 没有越权访问，所有API正确拒访
- [ ] **签字通过**

---

#### 2️⃣ 只读数据行无写操作按钮

**检查状态**：☐ PASS  ☐ FAIL  ☐ PARTIAL  ☐ N/A

**检查场景**：
- [ ] 场景A：结案案件
  ```
  案件ID: ______  状态: 已结清
  角色: VL
  
  观察：
  □ 能否进入详情页? 
  □ 详情页中是否隐藏了"标注/标记/转质检"按钮?
  □ 尝试点击操作按钮(若存在)，是否被禁用或拒绝?
  ```

- [ ] 场景B：其他物业批次
  ```
  批次ID: ______  所属物业: _____________
  当前角色: PL(物业A)
  
  观察：
  □ 列表中是否看不到该行，还是看到但标记为只读?
  □ 若看到，是否隐藏了"导入/编辑/删除"按钮?
  □ 点击API /batch/{id}/edit，是否被拒?
  ```

- [ ] 场景C：脱敏数据行的操作
  ```
  案件列表中结案案件行
  观察业主联系方式：_________________ (应脱敏为 ***)
  
  业主联系方式列上的"拨号"按钮: ☐ 隐藏  ☐ 灰显  ☐ 可用
  预期: 应隐藏或被禁用
  实际: ___________________________________
  ```

**代码检查**：
```vue
<!-- 检查前端是否有行权限检查 -->
<v-data-table>
  <template #item.action="{ item }">
    <!-- ✗ 不安全 -->
    <button v-if="can('case.mark')">标注</button>
    
    <!-- ✓ 安全 -->
    <button v-if="can('case.mark') && canEditThisRow(item)">标注</button>
  </template>
</v-data-table>

搜索关键词: _________________________________
搜索结果行号: ___________________________
```

**证据附件**：
- 前端列表组件截图（体现隐藏的按钮）
- 后端API拒访日志
- 代码片段

**结论**：
- [ ] 所有脱敏/只读行都无写操作按钮
- [ ] 点击已隐藏的按钮无效或被拒
- [ ] **签字通过**

---

#### 3️⃣ 查询语句含租户过滤

**检查状态**：☐ PASS  ☐ FAIL  ☐ PARTIAL  ☐ N/A

**检查方法**：

- [ ] 列举3个关键查询接口
  ```
  接口1: GET /api/cases
  接口2: GET /api/batches/{batchId}/cases
  接口3: GET /api/stats/recovery-rate
  ```

- [ ] 对每个接口，grep后端DAO/Service源码
  ```bash
  # 接口1: /api/cases
  grep -A 5 "getCases\|findCases" backend/src/service/*.ts
  
  搜索关键词: property_id / org_id / dataRange
  检查结果: ☐ 找到WHERE过滤  ☐ 未找到  ☐ 过滤条件不对
  ```

- [ ] 验证SQL语句结构
  ```sql
  -- 不安全 ✗
  SELECT * FROM cases WHERE status='进行中'
  
  -- 安全 ✓
  SELECT * FROM cases 
  WHERE status='进行中' 
  AND property_id=#{propertyId}
  
  实际代码中: ___________________________________
  ```

- [ ] 验证SE的数据范围过滤
  ```sql
  -- SE应仅见授权范围内数据
  SELECT * FROM cases 
  WHERE property_id IN (
    SELECT id FROM properties 
    WHERE id IN (SELECT property_id FROM se_datarange WHERE se_id=#{userId})
  )
  
  是否实现: ☐ 是  ☐ 否  ☐ 部分实现
  ```

- [ ] 验证统计/聚合查询
  ```sql
  -- 统计前必须先过滤
  SELECT COUNT(*), SUM(amount) 
  FROM cases 
  WHERE property_id=#{propertyId}  ← 范围过滤在GROUP前
  GROUP BY status
  
  是否正确: ☐ 是  ☐ 否
  ```

- [ ] 验证跨表JOIN
  ```sql
  -- 每个表都需范围限定
  SELECT * FROM cases c
  JOIN batches b ON c.batch_id = b.id AND b.property_id=#{propertyId}
  WHERE c.property_id=#{propertyId}
  
  正确性: ☐ 正确  ☐ 缺少范围  ☐ 逻辑错误
  ```

**证据附件**：
- SQL语句截图（行号、文件名）
- 后端Service方法代码
- 权限/数据范围参数传递链路文档

**结论**：
- [ ] 所有SELECT都包含明确的租户/范围过滤
- [ ] SE查询都经过授权范围校验
- [ ] 统计在GROUP/聚合前已过滤
- [ ] **签字通过**

---

#### 4️⃣ 高危操作二次确认+受控原因

**检查状态**：☐ PASS  ☐ FAIL  ☐ PARTIAL  ☐ N/A

**高危操作清单**：
```
☐ 撤案
☐ 坏账结案
☐ 冲正回款
☐ 作废案件（待派单）
☐ 批次关闭
☐ 服务商停用
```

**对每个操作验证**：

**操作：撤案**
- [ ] 点击"撤案"按钮，是否弹出Modal?
  ```
  Modal标题: ___________________________________
  Modal内容: ___________________________________
  ```

- [ ] Modal中"原因"字段类型
  ```
  ☐ <select>下拉(正确)  ☐ <input type="text">自由文本(错误)
  
  可选值: ___________________________________
  ```

- [ ] 是否能在未选择原因的情况下提交?
  ```
  测试：不选原因直接点"确认"
  预期: 提交按钮禁用 (:disabled=true)
  实际: ___________________________________
  ```

- [ ] 后端是否校验原因值?
  ```bash
  grep -A 10 "revokeCase\|doRevoke" backend/src/service/*.ts | grep -i "reason"
  
  校验逻辑: ___________________________________
  是否有: validate(reason IN CFG.REVOKE_REASONS)?
  ```

- [ ] 审计日志是否记录原因?
  ```
  操作日志中的reason字段: ___________________________________
  是否可追溯: ☐ 是  ☐ 否
  ```

**操作：结案**
```
重复上述检查...
```

**操作：冲正回款**
```
额外检查：
□ 是否显示"此操作不可逆"的警告?
□ 是否要求输入"操作确认码"或二次确认?
□ 日志中是否标记为"high-risk"?
```

**证据附件**：
- Modal弹窗截图
- 原因下拉选项列表
- 后端校验代码
- 审计日志示例

**结论**：
- [ ] 所有高危操作都有二次确认Modal
- [ ] 原因字段为受控枚举
- [ ] 提交前验证了选择
- [ ] 后端校验了原因合法性
- [ ] 审计日志完整记录
- [ ] **签字通过**

---

#### 5️⃣ 批量操作逐项权限检验

**检查状态**：☐ PASS  ☐ FAIL  ☐ PARTIAL  ☐ N/A

**批量操作清单**：
```
☐ 批量派单
☐ 批量标记风险
☐ 批量标注
☐ 批量下载
☐ 批量标记(导出)
```

**检查操作：批量派单**

- [ ] 前端勾选限制
  ```
  选择3个案件（其中1个状态为"已派单"，应无法勾选）
  
  能否勾选"已派单"的行?
  ☐ 能(错误)  ☐ 不能(正确)  ☐ 能勾但禁用(接受)
  
  代码检查 <checkbox :disabled="!canDispatch(item)">
  ```

- [ ] 后端逐项校验
  ```bash
  grep -A 20 "batchDispatch\|batch.*dispatch" backend/src/service/*.ts
  
  是否有 for 循环逐项检查?
  ☐ 是  ☐ 否  ☐ 有但不完整
  
  循环内检查项: ___________________________________
  - 权限? ☐ yes  ☐ no
  - 数据范围? ☐ yes  ☐ no  
  - 状态? ☐ yes  ☐ no
  ```

- [ ] targetVL授权校验
  ```
  POST /api/case/batch-dispatch
  {
    caseIds: [1, 2, 3],
    targetVL: "vendor-xyz"
  }
  
  后端是否验证: targetVL IN user.authorizedVLs?
  ☐ 是  ☐ 否
  
  代码位置: ___________________________________
  ```

- [ ] 实际测试
  ```
  勾选2个案件，派给"未授权的"服务商
  
  预期: API拒绝 (400/403)
  实际: ___________________________________
  ```

**证据附件**：
- 前端勾选框代码 (:disabled条件)
- 后端for-loop逐项校验逻辑
- API请求/响应日志

**结论**：
- [ ] 前端勾选仅允许有权行
- [ ] 后端for-loop逐项校验
- [ ] 目标对象必须被授权
- [ ] **签字通过**

---

#### 11️⃣ 结案脱敏规则全覆盖

**检查状态**：☐ PASS  ☐ FAIL  ☐ PARTIAL  ☐ N/A

**测试场景：选一个已结清的案件**
```
案件ID: ______
状态: 已结清
当前角色: VL / CO

以下逐一测试...
```

- [ ] 详情页重定向
  ```
  操作：点击案件进入详情
  
  预期: 自动跳转到"脱敏统计视图" (如 /case/{id}/stats)
  实际: ___________________________________
  
  能否直链进原详情页 /case/{id}?
  ☐ 能(错误)  ☐ 不能(正确)  ☐ 进去后再转(接受)
  ```

- [ ] 统计视图内容检查
  ```
  脱敏统计视图中显示: ___________________________________
  
  是否仅显示计数/汇总，无完整明细?
  ☐ 是(正确)  ☐ 否(错误)
  
  具体例子: ___________________________________
  ```

- [ ] 列表行脱敏检查
  ```
  在案件列表中找该结清案件行
  
  业主联系方式: _______________  (应为 *****)
  是否脱敏: ☐ 是  ☐ 否
  
  其他敏感字段(减免/律师函): _______________
  是否脱敏: ☐ 是  ☐ 否
  ```

- [ ] 子标签页隐藏检查
  ```
  若直接进原详情页(绕过redirect)
  
  通话&转写标签页: ☐ 隐藏  ☐ 可见(错误)
  承诺标签页: ☐ 隐藏  ☐ 可见(错误)
  工单标签页: ☐ 隐藏  ☐ 可见(错误)
  ```

- [ ] 代码检查
  ```
  检查详情页组件 mounted/created 逻辑：
  
  是否有: if (case.isClosed && ['VL','CO'].includes(role)) 
           { this.$router.push('/stats') }
  
  ☐ 有  ☐ 没有  ☐ 逻辑错误
  
  代码位置: ___________________________________
  ```

**证据附件**：
- 脱敏统计视图截图
- 列表行脱敏字段截图
- 详情页redirect代码
- 代码行号

**结论**：
- [ ] 结案后自动转脱敏视图
- [ ] 直链无法绕过
- [ ] 列表敏感字段脱敏
- [ ] 子标签页隐藏
- [ ] **签字通过**

---

#### 12️⃣ 用户故事全链路走查

**检查状态**：☐ PASS  ☐ FAIL  ☐ PARTIAL  ☐ N/A

**场景：催收员(CO)完整结案流程**

```
用户：CO(催收员)
目标：找一个进行中的案件，标注跟进，登记承诺，结案

开始时间: ______  结束时间: ______  耗时: ______ min
```

**Step 1：登录→工作台**
```
☐ 登录成功
☐ 工作台显示"今日待标注"列表
☐ 列表仅显示"本商公海+私海"的案件 (非全平台)

验证方法：
  打开浏览器控制台 → Network
  查看 GET /api/case/my-todos 的请求参数中是否有 dataRange/vendorId
  是否有: ☐ 是  ☐ 否
```

**Step 2：打开案件详情**
```
操作：点击一个待标注案件进入详情

☐ 详情页加载成功
☐ 能看到"通话&转写"标签页内容
☐ 能看到"承诺"标签页
☐ 能看到"标注"按钮(应可见)

验证：
  权限检查代码: can('case.mark')? ___________________________________
```

**Step 3：标注跟进**
```
操作：点击"标注跟进"

☐ 弹出跟进表单
☐ 能填写跟进结果（下拉或文本）
☐ 点提交后API成功 (200)
☐ 操作日志记录了该标注

验证：
  操作日志中: operator=CO, action=标注跟进, caseId=____
  是否记录: ☐ 是  ☐ 否
```

**Step 4：登记承诺**
```
操作：点击"登记承诺"

☐ 弹出承诺表单（支持单笔/分期）
☐ 填写承诺金额和日期
☐ 提交成功
☐ 案件承诺列表更新

代码检查: @Perm('case.promise') on API endpoint?
是否有: ☐ 是  ☐ 否
```

**Step 5：结案**
```
操作：点击"结案"按钮

☐ 弹出二次确认Modal
☐ Modal中有"原因"下拉(非文本)
☐ 选择原因后点"确认"
☐ 案件状态变为"已结清"
☐ 页面跳回列表

验证：
  操作日志: operator=CO, action=结案, reason=____
  审计记录完整: ☐ 是  ☐ 否
```

**Step 6：返回列表，再进该案件**
```
操作：在列表中再次点击该已结清案件

☐ 自动跳转到脱敏统计视图 (如 /cases/{id}/stats)
☐ 不是原详情页
☐ 仅显示计数/转化率统计，无完整明细

这是最关键的检查点！
是否成功脱敏重定向: ☐ 是  ☐ 否 ← 任何一个"否"就审计失败
```

**Step 7：查操作日志**
```
操作：进案件详情(或统计页)→操作日志标签

☐ 能看到"标注"操作记录
☐ 能看到"承诺"操作记录
☐ 能看到"结案"操作记录
☐ 仅显示与本案相关的日志(非跨商泄漏)
☐ 不显示其他CO的操作(仅本人或管理员可见)

跨商隐藏检查：
  操作日志SQL是否有: WHERE org_id=#{vendorId}?
  是否有: ☐ 是  ☐ 否
```

**整体评分**：

```
Step完成度: 1☐ 2☐ 3☐ 4☐ 5☐ 6☐ 7☐

全部通过(7/7): ☐ 一级 - 整体正常
部分失败(<=2个): ☐ 二级 - 需修复
失败>2个: ☐ 三级 - 严重缺陷
```

**证据附件**：
- 完整的流程截图序列
- 浏览器Network日志(关键API请求)
- 最终操作日志截图

**结论**：
- [ ] 所有7个环节都通过检查
- [ ] 权限隔离和脱敏规则贯穿全流程
- [ ] **签字通过**

---

### 🟠 MEDIUM优先级（4项）

#### 6️⃣ 代理操作审计日志有痕迹
**检查状态**：☐ PASS  ☐ FAIL  ☐ PARTIAL  ☐ N/A

**测试场景**：
- [ ] SA切换角色选择"代理PL-A"
- [ ] 在代理模式下结案一个案件
- [ ] 查看该操作的审计日志
  ```
  日志内容应包含:
  - operator: SA-xxx
  - proxyFor: PL-yyy (← 关键！)
  - targetOrg: 物业A
  - action: 结案
  - timestamp: ...
  
  实际记录: ___________________________________
  proxyFor字段存在: ☐ 是  ☐ 否
  ```

#### 7️⃣ 跨端权限口径一致
**检查状态**：☐ PASS  ☐ FAIL  ☐ PARTIAL  ☐ N/A

**测试**：
- [ ] PC端和App端CO的菜单是否相同 (或文档说明差异)
- [ ] 同一CO在PC结案 vs App结案，脱敏规则是否一致
- [ ] H5业主页的token是否有过期机制

#### 8️⃣ 权限点字典与代码绑定完整
**检查状态**：☐ PASS  ☐ FAIL  ☐ PARTIAL  ☐ N/A

**检查**：
```bash
# 列出权限字典
cat backend/src/config/permissions.ts | grep "case\."

# 逐个验证后端装饰器
grep -rn "@Permission\|@Perm" backend/src/ | wc -l

# 检查前端can()调用
grep -rn "can(" frontend/src/ | grep -o "can('[^']*" | sort -u

# 对比：权限字典 vs 后端装饰器 vs 前端can()
# 三者应对齐
```

**结论**：
- [ ] 权限字典完整
- [ ] 后端装饰器覆盖全部权限
- [ ] 前端can()调用对应权限字典
- [ ] 无孤岛权限(定义但未用)

#### 10️⃣ API参数目标对象校验
**检查状态**：☐ PASS  ☐ FAIL  ☐ PARTIAL  ☐ N/A

**测试派单API**：
```bash
curl -X POST http://localhost:8080/api/case/dispatch \
  -H "Authorization: Bearer {token}" \
  -d '{
    "caseIds": [123, 456],
    "targetVL": "unauthorized-vendor"
  }'

预期: 403 Forbidden(targetVL未授权)
实际: ___________________________________
```

---

### 🟡 LOW优先级（1项）

#### 9️⃣ 演示数据覆盖多租户
**检查状态**：☐ PASS  ☐ FAIL  ☐ PARTIAL  ☐ N/A

**检查**：
```bash
# Mock物业数量
grep -o '"propertyId":"[^"]*"' frontend/mock.json | sort -u | wc -l
结果: _________ 个 (应≥3)

# Mock服务商数量  
grep -o '"vendorId":"[^"]*"' frontend/mock.json | sort -u | wc -l
结果: _________ 个 (应≥3)
```

---

## 总体审计结论

### 检查项统计

| 等级 | 项目数 | 通过 | 失败 | 部分 | 签字 |
|---|---|---|---|---|---|
| 🔴 HIGH | 7 | ☐☐☐☐☐☐☐ | | | |
| 🟠 MEDIUM | 4 | ☐☐☐☐ | | | |
| 🟡 LOW | 1 | ☐ | | | |
| **总计** | **12** | | | | |

### 总体评分

```
评分规则：
  • 所有HIGH + MEDIUM = 11项全通过 → 一级(满分)
  • HIGH全通过 + MEDIUM ≤1个失败 → 二级(可交付)
  • HIGH有2-3个失败 → 三级(需修复)
  • HIGH >3个失败 → 一级缺陷(禁交付)

本次审计：
  HIGH通过: ___/7
  MEDIUM通过: ___/4
  LOW通过: ___/1
  
  总体等级: ___级
  
  建议: ___________________________________
```

### 缺陷清单

```
缺陷1：
  检查项：#____
  描述：___________________________________
  严重性：☐ HIGH  ☐ MEDIUM  ☐ LOW
  修复优先级：第 __ 位
  预计工作量：____ h
  
缺陷2：
  ...
```

### 签字确认

```
审计人签字：________________  日期：__________

审批人签字：________________  日期：__________

是否同意上线：☐ 是  ☐ 否  ☐ 条件通过

备注：___________________________________
```

---

## 附录

### A. 快速检查命令手册

```bash
# 检查SQL WHERE子句
grep -n "SELECT.*FROM" backend/src/dao/*.ts | grep -v "WHERE.*org_id"

# 检查权限装饰器覆盖
find backend/src/controller -name "*.ts" -exec grep -l "async" {} \; | \
  xargs grep -L "@Permission\|@Perm" | head

# 检查脱敏规则
grep -rn "isClosed\|redact\|mask" frontend/src/ | grep -i "case\|phone" | head

# 检查批量逐项校验
grep -A 20 "batch.*dispatch\|batchUpdate" backend/src/service/*.ts | grep -E "for|while"
```

### B. 权限检查清单(权限点字典映射)

| 权限点 | 后端装饰器 | 前端检查 | 状态 |
|---|---|---|---|
| case.dispatch | ☐ 有 | ☐ 有 | ☐ OK |
| case.mark | ☐ 有 | ☐ 有 | ☐ OK |
| case.promise | ☐ 有 | ☐ 有 | ☐ OK |
| case.evidence | ☐ 有 | ☐ 有 | ☐ OK |
| case.close | ☐ 有 | ☐ 有 | ☐ OK |
| ... | | | |

### C. 高危操作确认清单

- [ ] 撤案：二次确认 + 原因枚举
- [ ] 结案：二次确认 + 原因枚举
- [ ] 冲正回款：二次确认 + 不可逆提示
- [ ] 作废：二次确认 + 原因
- [ ] 删除：二次确认 + 原因

### D. 数据隔离验证记录

```
SE-Alice (授权范围: 阳光花园+望京社区)

查询 GET /api/case
预期返回案件所属物业: 阳光花园 / 望京社区
不应返回: 其他物业案件

实际结果: ___________________________________
是否隔离正确: ☐ 是  ☐ 否
```

---

**版本**：v1.0  
**更新时间**：2026-06  
**项目**：有证慧催  
**模板制作人**：_______________

