# 前端构建期环境变量。拷贝为 .env.local 后修改（.env.local 不入库）。
#
#   cp frontend/.env.example frontend/.env.local

# 催收员 App 安装包地址。
# 不设时默认取同源的 /app/huicui.apk —— 也就是「APK 放在前端静态资源目录下的 app/ 里」。
# 见 deploy/README.md 第六节。
#
# 本机联调时，APK 通常单独起个服务托管，填它的地址：
#   VITE_APP_DOWNLOAD_URL=http://192.168.1.100:8899/huicui.apk
#
# 生产放对象存储/CDN 时：
#   VITE_APP_DOWNLOAD_URL=https://cdn.example.com/huicui-v1.0.apk
VITE_APP_DOWNLOAD_URL=
