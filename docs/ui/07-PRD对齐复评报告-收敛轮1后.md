# 07 · 有证慧催 PRD 对齐复评报告（收敛轮1 后）

> 复评对象：`/Users/shuo/AI/cuishou/docs/ui/高保真/`（index.html 2613 行 / app.html 309 行 / h5.html 445 行 / login.html 228 行）
> 对齐基准：PRD 11 模块（`/docs/prd/00~10`，US-M*/BR-M* 共 220 条）+ `/docs/ui/02-角色操作权限矩阵.md`
> 方法：综合 2 组并行审计（Q1 覆盖审计 / Q2 角色一致性审计）后，**评审综合者对所有 high / 可疑 missing / over-permission 项亲自 grep+Read 当前文件对抗校验**，修正误报/漏报后纳入。所有行号、单元格均以当前文件实际取证为准，不照抄旧报告。
> 日期：2026-06-19 · 复评轮次：收敛轮1（FW1+FW2）之后

---

## ① 执行摘要

收敛轮1 后，原型在权限子集授权、数据范围授权、报表按 scope 过滤+导出留痕、结案受控、对账聚合计费、分期承诺逐期标记、证据材料打包、跨组织兼职展示等方向均落地，整体已逼近"可进入接口契约/开发阶段"的成熟度。**但对抗校验推翻了上一轮审计中分量最重的一条 high 误报**（详见 ④），同时确认了若干真实的角色一致性缺口（SE 平台公海/存证/参数/审计入口、对账单写控件对 SA/SE 过宽、开放抢单付佣无防倒挂、若干 CFG 未进配置表、doClose 不写 auditLog）。

### 总体评分（百分制）

| 维度 | 本轮得分 | 收敛前基线(06) | 更早(05) | 提升 |
|---|---|---|---|---|
| **Q1 PRD 功能实现度** | **88** | 75 | 67.9 | +13 |
| **Q2 角色权限一致性** | **90** | 86 | — | +4 |

> Q1=88 由各模块覆盖率按故事数加权：M0 92 / M1 87 / M2 88 / M3 82 / M4 85 / M5 83 / M6 92 / M7 97 / M8 72 / M9 88 / M10 85，整体约 88。
> Q2=90：6 角色中 SA/PL/PC/VL/CO/业主H5 对齐度高（94~98），SE 因 3~4 项 nav 入口缺失/过宽拉低至约 86，加权后 90。

### 本轮相对收敛前（75/86）的提升点

1. **PC/SE/VL 角色入口补齐**：PC 获 legal/evidence/reconIn 入口；VL 获 reconOut/coCommission/recharge/billing；perms 与矩阵 §3.2 逐格对齐。
2. **数据范围授权全链路落地**：SE `dataRange` 驱动 `qcVisible/alertVisible/auditVisible/membersVisible/scopedProjects/visibleCases/filteredCases` 七处 computed 裁剪（L2286-2384）。
3. **报表按 scope 过滤 + 导出留痕**：`exportReport()`（L2435）写 auditLog（actor/time/scope），`report.export` 权限门控，PC/CO 无入口。
4. **结案受控收敛**：`canSeeDetail`（L2365）+ `redactClosed`（VL/CO=true）+ P-CLOSE-02 统计视图 + `field()` 列表态脱敏，双端（index/app）一致。
5. **对账聚合计费明细 + 结算单据抽屉**：`openSettle()`（L2433）含回款下钻/计费明细/电子章/打款凭证；`billingVisible`（L2293）服务商仅见 STT/分钟。
6. **分期承诺录入 + 逐期标记 + 证据材料打包 + 开放抢单付佣弹窗 + 防倒挂校验**（普通付佣路径，L238-240/L2509）。
7. **资金双线物理隔离**完整：项目档案/批次佣金区块 v-if 角色组硬隔离（L115/234-235），物业↔服务商零互通。

---

## ② Q1 逐模块覆盖

### 覆盖表

| 模块 | 已实现 | 部分 | 缺失 | 覆盖率 |
|---|---|---|---|---|
| M0 总纲 | 11 | 1 | 0 | 92% |
| M1 组织与权限 | 7 | 2 | 0 | 87% |
| M2 项目/批次/案件主数据 | 6 | 2 | 0 | 88% |
| M3 案件流转与公海 | 8 | 3 | 0 | 82% |
| M4 催收作业 | 8 | 4 | 0 | 85% |
| M5 录音AI（STT/策略/质检） | 4 | 2 | 0 | 83% |
| M6 存证（三场景/计费/核验） | 5 | 1 | 0 | 92% |
| M7 业主缴费 H5 | 3 | 0 | 0 | 97% |
| M8 结案与生命周期 | 2 | 4 | 0 | 72% |
| M9 对账结算 | 10 | 3 | 0 | 88% |
| M10 经营报表 | 7 | 2 | 0 | 85% |
| **合计** | **71** | **24** | **0** | **≈88%** |

> 无"完全缺失"故事项。所有缺口集中在"部分实现"（差异化逻辑/汇总维度/枚举对齐/留痕落库）。原型态可接受占位（佣金 TBD、图表占位、上链哈希演示值、token 客户端白名单、并发原子锁后端化）不计入缺口。

### 真实缺口清单（按 severity 排序，已经过对抗校验）

| # | severity | US/BR | 缺口 | UI 证据 |
|---|---|---|---|---|
| Q1-1 | **medium** | BR-M8-02/03/04/05（US-M8-02/03） | `doClose()`（L2560）仅 `toast`，**不写 auditLog、不更新 `case.status` 为撤案/坏账**；SA 代结案无 `proxyFor`，被代物业操作日志看不到该条目；`CFG-CLOSE-REASONS` 未进 `cfgs`（L2207），SA 无法从设置 UI 配置结案原因枚举 | index.html L2560 / L2207 |
| Q1-2 | **medium** | BR-M5-07（US-M5-06） | 风险处置弹窗仅"标记/转质检/通知督导"，**无"生成处置待办并推送服务商要求整改/培训"动作**，无处置任务跟踪状态，闭环未成 | index.html L2043-2066 |
| Q1-3 | **medium** | BR-M5-05（US-M5-04） | 作战手册版本迭代：**缺版本历史列表、缺新旧版 diff 高亮弹窗**（仅 toast 占位）、缺"录入项目相关情况"驱动 AI 的专用字段 | index.html L121-135 |
| Q1-4 | **medium** | BR-M4-17（US-M4-06） | 工单类型枚举与 PRD **不符**：PRD 定义『上门核实/材料证明/法务工单/其他』，原型为『上门核实/信息核对/争议处理/减免申请/其他』，缺『材料证明』『法务工单』，多 3 个未定义项 | index.html L1604-1609（已核 PRD 04 L62） |
| Q1-5 | **medium** | BR-M9-14/18 | **开放抢单付佣比例无防倒挂校验**：`saveOpenCommission()`（L2541）仅校验 `rate>0`，未与该批次 `recvRate` 比较；普通付佣路径（L2509 rateInvalid）已有保护，开放抢单路径缺 | index.html L2541 |
| Q1-6 | **medium** | BR-M10-01/02（US-M10-01） | 平台报表**缺区域/周期维度聚合下钻**：仅项目→批次下钻，无独立区域筛选器或日期/周期范围选择控件 | index.html L1172-1199 |
| Q1-7 | **medium** | US-M10-02 | 物业报表**缺佣金支出+能力计费汇总**：PL reports（L1058-1112）有回款率/周期/撤案/服务商表现，但无佣金支出聚合与 STT/短信/存证费用汇总（数据散在 reconIn/billing 视图，未整合进报表） | index.html L1058-1112 |
| Q1-8 | **medium** | BR-M1-13 / US-M1-03 AC3 | 跨组织兼职**仅只读展示（L1256-1277），无创建入口**：负责人侧添加同手机号员工时"允许创建为本组织独立账号"的交互/提示缺失 | index.html L1256-1277 |
| Q1-9 | **medium** | BR-M2-17 | 案件级单独作废入口缺失（仅批次级弹窗 L158/L1847）；PC `case.void` **未带 ▲ 数据范围门控**（仅靠 visibleCases 间接隔离） | index.html L158 / L2131 |
| Q1-10 | **medium** | BR-M4-03 / BR-M3-08 / BR-M8-11 | **有效/无效跟进对 T_collector 的差异化未在 UI 体现**；有效分期承诺期间暂缓自动释放（BR-M8-11）无任何逻辑，仅 `case.mark` 统一 toast 重置 | index.html L589 |
| Q1-11 | **medium** | BR-M4-12 | 标注枚举 `CFG-MARK-CODES` 超管配置缺失：settings（L962）无此配置项，SA 无法配置标注枚举 | index.html L962 / L2207 |
| Q1-12 | **medium** | BR-M4-11 / 多号码 | 无效号码"置灰默认不外呼"未体现；通话栏只展示单主号，无多号码切换拨打面板 | index.html L498-531 |
| Q1-13 | **medium** | BR-M3-13a | 服务商滞留临近 T2 仅看板汇总数，**缺单案件粒度"某案即将退回"推送/预警** | index.html L370/L2415 |
| Q1-14 | **medium** | BR-M9-03 / US-M9-03 | 通话分钟充值预警/暂停阈值 `CFG-MIN-WARN/STOP` 未进配置表；SA 代充值无专用代操作留痕入口 | index.html L825-860 / L2207 |
| Q1-15 | **low** | BR-M8-01 | 分期累计到账判定缺失：`doRepay()`（L2436）仅单笔全额判已结清，无逐期 `installmentPaid` 累计逻辑；结案触发来源未落库 | index.html L2436 |
| Q1-16 | **low** | US-M2-03 | 批次导入仅 Excel 向导，无手工逐条新增表单入口 | index.html L167-190 |
| Q1-17 | **low** | BR-M9-08 | 短信失败仅逐行状态，缺各原因类型汇总统计 | index.html L946-958 |
| Q1-18 | **low** | BR-M5-09 | 训练/样本脱敏合规 UI 提示完全缺失（原型态可接受占位） | index.html 全文无 |
| Q1-19 | **low** | BR-M0-… / BR-M1-08 | 多数案件操作（标注/发链接/分配）仅 toast 不写 auditLog，全程留痕覆盖不完整 | index.html L2507 等 |

---

## ③ Q2 角色一致性

### 6 角色 × 一致性总览

| 角色 | 分数 | nav 对齐 | perms 对齐 | scope 过滤 | 结案脱敏 | 关键问题 |
|---|---|---|---|---|---|---|
| SA 平台超管 | 98 | ✓ 全分组 | ✓ 14 项=§3.2 | scope=all | redactClosed=false ✓ | 仅对账单写控件过宽（见 Q2-2） |
| SE 平台员工 | 86 | **缺 platformSea/orgMgmt/settings/audit；多 evidence** | ✓ 3 项 | dataRange 七处裁剪 ✓ | false ✓ | **本轮主要不一致集中角色** |
| PL 物业负责人 | 97 | ✓ | ✓ 7 项=§3.2 | scope=property | false ✓ | reconIn 静态数据未显式 scope 过滤（演示态低风险） |
| PC 物业协调员 | 96 | ✓（无 members，符合§8） | ✓ 11 项=§3.2 | scope=property | false ✓ | case.reduce ▲ 靠金额阈值间接实现（可接受） |
| VL 服务商负责人 | 94 | ✓（正确排除 reconIn/evidence/sms/settings/playbookLib） | ✓ 2 项=§3.2 | scope=provider 全过滤 | redactClosed=true ✓ | 批量退回按钮无门控（轻）；evidenceVisible 无 provider 分支（latent，nav 已排除入口） |
| CO 催收员 | 97 | ✓ 仅 3 项 | ✓ 7 项=§3.2 | scope=provider | true ✓ | providerSea 批量退回工具栏对 CO 可见（轻 over-permission） |
| 业主 H5（无登录） | 98 | token 门控 | — | — | — | token 客户端白名单（OQ-M7-2，生产需后端签发） |

### 专项核对结论

- **资金双线隔离（§6/§9）✓**：`billingVisible`（L2293）provider→仅 STT/分钟；VL nav 无 reconIn/sms，PL nav 无 reconOut/coCommission；项目档案佣金区块 `v-if=['SA','SE','PL','PC']`（L115）/批次付佣 `v-if=['VL','SA','SE']`（L235）双向硬隔离；结算计费明细仅 reconIn 侧。**通过**。
- **结案脱敏（BR-M8-09）✓**：index `canSeeDetail`（L2365）+ app `_redacted`（L88/163）双端一致，VL/CO 结案后 `caseOps=[]`、明细标签消失、切 P-CLOSE-02。**通过**。
- **SE 数据范围（BR-M1-14）✓ 落地，但功能入口有缺**：七处 computed 裁剪正确；缺 platformSea/orgMgmt/settings/audit 独立入口（见不一致清单）。
- **PC/PL 权限边界 ✓**：PL=proj.edit 有、case.legal.start 无（法务由 PC 主导）；PC=case.legal.start/evidence/repay/reverse 有、proj.edit 无；对账单 PC 仅下载（`role!=='PC'` 屏蔽写）。**通过**。
- **CO/VL 私海公海可见性 ✓**：CO myCases=`owner==='王五'&&进行中`；providerSea 本商池/开放抢单 seaTab 隔离；跨服务商不可见。**通过**（仅批量退回工具栏门控待补）。
- **业主 H5 最小可见（BR-M7-07）✓**：H5 数据字段无『安信催收/服务商/催收员/佣金/案件编号』，姓名脱敏『李\*\*』，`fill()` 用 `textContent` 防 XSS（L353），过期态不填充 PII。**通过**。

### 不一致清单（已对抗校验）

| # | 类型 | severity | PRD 依据 | UI 现状 | 建议 |
|---|---|---|---|---|---|
| Q2-1 | missing-access | **high** | 02 §5 L95：平台公海（再派/开放抢单）**SE=●▲** | SE nav（L2125）有 `dispatch` 但**无 `platformSea`**；dispatch/platformSea 共用模板（L397）分支渲染，SE 只看得到 T1 待派单池，看不到 T2 退回/超时的平台公海，无法处置积压 | SE nav 增加 `platformSea` 条目；按 dataRange 裁剪 |
| Q2-2 | over-permission | **medium** | 02 §6：对账单确认/驳回/申诉 **SA=○、SE=○▲（只读）** | 确认/驳回按钮（L768-769）仅 `v-if="r.status!=='已确认'&&role!=='PC'"`，**SA/SE 均可点击写操作**；申诉提交/标记同（L787-788） | 改为 `view==='reconIn'?role==='PL':role==='VL'`，SA/SE 只读可见不可写 |
| Q2-3 | over-permission | **medium** | 02 §7 L129：存证管理 **SE=—** | SE nav（L2125）**含 `evidence`**，可只读浏览全部存证列表；写操作虽被 `can('case.evidence')` 门控（SE 无此 perm），但 nav 入口+只读可见本身即越矩阵 | SE nav 移除 `evidence`；或与业务确认改 §7 SE=○ 后保留只读 |
| Q2-4 | missing-access | **medium** | 02 §8 L143：成员管理 **SE=●▲**；参数配置/角色模板/短信模板 SE=●▲（若授权） | SE nav（L2125）**无 `orgMgmt`、无 `settings`**；roleSnapshot 注释称"对齐§8"但 §8 原文 SE=●▲ | 补 orgMgmt/settings 入口（settings 标注"默认不授权"而非缺失），叠加 ▲ |
| Q2-5 | missing-access | **medium** | 02 §3.1：操作日志 SE=○▲；BR-M1-15 代操作透明 | SE nav（L2125）**无独立 `audit`** 汇总页（PL/PC/VL/SA 均有）；`auditVisible`（L2290）已有 range 过滤逻辑，仅差入口 | SE nav 增加 `audit` 条目 |
| Q2-6 | over-permission | **low** | BR-M3-28：批量退回属服务商负责人（VL）管理动作；CO 应走 per-case 释放 | providerSea 工具栏 `批量退回公海` 按钮（L360）**无 `can()` 门控**，CO 可在 seaTab=own 多选后看到（per-case 主动退回 L375 已正确门控 VL/CO） | 给批量退回按钮加 `v-if="['VL','SA'].includes(role)"` |
| Q2-7 | latent code（over-permission+scope-mismatch） | **low** | 02 §7/§9：服务商不涉存证 | `evidenceVisible`（L2295）**无 `provider` 分支**，VL/CO scope=provider 时 fallback 返回全量 `evidenceRows`；当前 VL/CO nav 均无 evidence 入口故**实际不可达、不泄露**，但后端复用此逻辑会越权 | 补 `if(sc==='provider')return []` 防御性分支 |
| Q2-8 | scope-mismatch | **low** | 02 §6：收佣对账 PL=○本物业（数据隔离硬约束） | `reconIn`（L2205）为静态数组直接渲染，无 per-role scope 计算属性；演示仅一条本物业数据，多物业上线需补过滤 | 后端实现时按 property 过滤 |
| Q2-9 | open-question | **low** | OQ-M7-2 / BR-M7-01 | H5 token 为客户端 IIFE 白名单硬编码（demo/demo-installment/demo-expired），生产需后端签发短期 JWT/HMAC 并后端验证 | 实现阶段补后端 token 签发与校验 |

---

## ④ 校验修正记录（被对抗校验推翻/修正的项）

> 以下为综合者亲自 grep+Read 当前文件后，对两组并行审计结论的**纠偏**，是本报告与上一轮审计的关键差异。

| 原审计结论 | 原 severity | 校验取证 | 修正后结论 |
|---|---|---|---|
| **【推翻】** Q1（M5+M6 块）：『VL 有 evidence nav 入口（L2133）且 evidenceVisible 未过滤 provider，双重违规』 | **high** | 实读 L2133：VL nav=`[workbench,providerSea,projects,qc,cases,reconOut,coCommission,recharge,billing,reports,members,audit]`，**不含 evidence**。该 high 误报源于把 SE 的 evidence 入口错记到 VL。 | **降级**：VL 不可达 evidence 视图。残留真实项为 `evidenceVisible` 缺 provider 分支的 latent 代码隐患（Q2-7，**low**）。Q2 审计对此判断正确（VL nav 正确排除 evidence），Q1 与 Q2 此处冲突，以 Q2+实测为准。 |
| **【修正/确认】** Q1 多处：『SE nav 缺 platformSea』标 medium；Q2：标 **high** | 不一 | 实读 02 §5 L95：平台公海 SE=**●▲**（明确授权处置）。SE nav（L2125）确无 platformSea。 | **确认为真实缺口**，severity 取 **high**（Q2-1）：这是 SE 唯一明确"应有写权能力"却无入口的项，影响 T2 积压处置闭环。 |
| **【新增/确认】** SE 含 evidence nav（§7 SE=—） | 两组均提及但定性不一 | 实读 L2125 SE nav 含 `evidence`；02 §7 L129 SE=—。 | **确认 over-permission**（Q2-3，medium）：纳入清单。 |
| **【确认】** doClose 不写 auditLog / 不改 status / CFG-CLOSE-REASONS 缺失 | medium | 实读 L2560：`doClose` 仅 toast；grep `CFG-CLOSE`/`CFG-MARK` 无命中。 | **确认为真**（Q1-1/Q1-11），纳入。 |
| **【确认】** 对账单确认/驳回仅 `role!=='PC'` 门控 | medium | 实读 L768-769/787-788：确为 `role!=='PC'`，SA/SE 可写。 | **确认 over-permission**（Q2-2），纳入。 |
| **【确认】** 开放抢单付佣无防倒挂 | medium | 实读 L2541 `saveOpenCommission`：仅 `rate>0`。 | **确认为真**（Q1-5），纳入。 |
| **【确认】** 工单枚举与 BR-M4-17 不符 | medium | 实读 L1604-1609 vs PRD 04 L62。 | **确认 matrix-divergence**（Q1-4），纳入。 |
| **【确认】** batchReturnSea 按钮无门控、CO 可见 | low-medium | 实读 L360：批量退回按钮无 `can()`；CO nav 含 providerSea。 | **确认轻度 over-permission**（Q2-6，low），纳入。 |
| **【维持】** 资金双线隔离 / 结案脱敏 / H5 最小可见 / SE dataRange 七处裁剪 | ok | 实读 L115/234-235/2293/2365、app L88/163、h5 L297-353、index L2286-2384。 | **维持 通过**，无修正。 |

> 漏报核查：未发现两组审计在 high 维度的额外漏报；M8 doClose 落库问题两组均已捕获。Q1/Q2 对 VL evidence 的冲突已在上表第 1 行裁定。

---

## ⑤ 结论与后续建议

**结论**：原型在收敛轮1 后达到 **Q1=88 / Q2=90**，较收敛前（75/86）与更早（67.9）显著提升，资金双线隔离、结案脱敏、SE 数据范围、PC/PL/CO/VL 权限边界、业主 H5 最小可见六大硬约束均已正确落地。**上一轮被标为 high 的"VL 越权看全量存证"经对抗校验为误报**（VL nav 不含 evidence），实际无 high 级可达漏洞。剩余缺口均为"部分实现"或入口缺失/门控过宽，无阻断性安全问题，可进入接口契约/开发阶段，建议并行修复下列收敛项。

**建议优先级（收敛轮2）**：

- **P0（角色矩阵硬对齐，改动小、风险高）**：
  1. SE nav 补 `platformSea / orgMgmt / settings / audit`（Q2-1/4/5），settings 标注"默认不授权"。
  2. SE nav 移除 `evidence` 或与业务确认 §7 改 SE=○ 只读（Q2-3）。
  3. 对账单确认/驳回写控件改为 PL(reconIn)/VL(reconOut) 专属，SA/SE 只读（Q2-2）。
  4. `evidenceVisible` 补 `provider` 防御分支；`批量退回公海` 按钮加 `['VL','SA']` 门控（Q2-6/7）。

- **P1（功能闭环/留痕）**：
  5. `doClose` 写 auditLog（含 status 更新、SA 代操作 proxyFor）；`CFG-CLOSE-REASONS`/`CFG-MARK-CODES`/`CFG-MIN-WARN/STOP` 进 cfgs（Q1-1/11/14）。
  6. 开放抢单付佣弹窗叠加防倒挂校验（Q1-5）。
  7. 工单类型枚举对齐 BR-M4-17『上门核实/材料证明/法务工单/其他』（Q1-4）。

- **P2（报表/手册/H5 增强）**：
  8. 平台报表加区域/周期筛选器；物业报表加佣金支出+能力计费汇总（Q1-6/7）。
  9. 作战手册版本历史+diff 高亮弹窗（Q1-3）；风险处置生成整改待办闭环（Q1-2）。
  10. 跨组织兼职创建入口/提示（Q1-8）；H5 生产侧后端 token 签发（Q2-9）。

> 文件路径锚点：`/Users/shuo/AI/cuishou/docs/ui/高保真/index.html`（角色 L2119-2138、scope computed L2286-2384、canSeeDetail L2365、recon 写控件 L768-789、evidence 视图 L935-942、doClose L2560、saveOpenCommission L2541、工单枚举 L1604-1609）、`app.html`（L88/163-164）、`h5.html`（L297-353）。
