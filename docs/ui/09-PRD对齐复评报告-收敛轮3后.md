# 有证慧催 · PRD 对齐复评报告（收敛轮3·FW5 后）

> 评审范围：`/Users/shuo/AI/cuishou/docs/ui/高保真/`（index.html ≈2989 行 / app.html / h5.html / login.html）
> 对齐基准：`/Users/shuo/AI/cuishou/docs/prd/`（00~10，220 条 US-M*/BR-M*）+ `/Users/shuo/AI/cuishou/docs/ui/02-角色操作权限矩阵.md`
> 复评方法：综合两路并行审计（Q1 覆盖审计、Q2 角色一致性审计），对所有 high/medium 与可疑 missing/over-permission 项**亲自 grep/Read 取证对抗校验**，修正后纳入。本报告**不照抄旧报告**，证据行号均为当前文件实取。
> 评审角色：评审综合者（opus）

---

## 一、执行摘要

经历"四批改造 + 收敛轮1（FW1/FW2）+ 收敛轮2（FW3/FW4）+ 收敛轮3（FW5）"后，原型在 6 角色权限矩阵、资金双线隔离、结案脱敏、代操作透明四条护城河上一致性极高，核心闭环（M0~M9）实现度全面拉升。但**收敛轮3 后仍有 5 处 medium 级缺口未修复**，是阻碍达到 ≥98 目标的主要短板，且均集中在"数据范围 / 角色门控的纵深过滤"而非功能缺失。

### 总分

| 维度 | 本轮（FW5 后） | 基线 08 | 基线 07 | 基线 06 |
|---|---|---|---|---|
| **Q1 总体实现度** | **94.5 / 100** | 92 / 93 | 88 / 90 | 75 / 86 |
| **Q2 总体角色一致性** | **96.8 / 100** | 93 | 90 | 86 |

> Q1 取 11 模块覆盖率加权均值（M0=98 / M1=92 / M2=93 / M3=93 / M4=94 / M5=95 / M6=97 / M7=98 / M8=94 / M9=98 / M10=95）。
> Q2 取 6 角色一致性均值（SA=99 / SE=95 / PL=98 / PC=97 / VL=97 / CO=97）。

### 收敛轮3（FW5）已确认落地的提升点（实取证）

| 提升点 | 证据（index.html） |
|---|---|
| doClose 补 proxyFor 且 auditVisible 命中代理行（SA 代结案被代物业可见 BR-M1-15） | L2927 `proxyOrg=(scope==='all')?cs.property` → logOp；L2583 auditVisible property 分支匹配 `proxyFor==='物业钱'\|\|'阳光物业'` |
| qc 高风险告警卡片按钮加 SA/SE 门控 | L985 `v-if="role==='SA'\|\|role==='SE'"`，否则 `— 只读(◐)` |
| 批次详情「发起结算」触发出单（全结案解锁+写对账+logOp） | L280 发起结算按钮 SA/SE；L2754 batchAllClosed 门控；L2755 startReconSettle 写 reconIn/Out |
| 结算单据加「发送对方对账/打印」 | settlement 抽屉；sendReconToCounterparty 写 logOp |
| evidence/billing 重试加 can() 门控 | L1028 evidence 重试 `v-if="can('case.evidence')"`；L929 billing 重试同 |
| 6 个 CFG 进 cfgs（COMM-IN/OUT/RECON-TRIGGER/LEGAL-FEE/SMS-COUNT/SMS-WARN） | L2492 cfgs 数组 |
| 建工单改绑 can('case.link')、VL 整改回填、批量补解析余额耗尽文案、存证独立核验、项目回款列、发缴费渠道弹窗、M0 商业模式/术语页 | L1009 VL 回填；L992 片段跳播；L2880 doBatchReparse；openEvidenceVerify；about/settings 视图 |

### 仍未达 98 的剩余阻碍清单（按 severity）

| # | severity | 模块 | 缺口 | 修复指引 |
|---|---|---|---|---|
| 1 | **medium** | M5 | BR-M5-07 质检处置：PL/PC 无"发起整改待办"入口（L993 仅 SA/SE） | L993 条件增 `\|\|role==='PL'`；处置任务卡 L997 增 PC 只读 |
| 2 | **medium** | M8 | US-M8-05 maxCycleCases 未按角色 scope 过滤，PL 弹窗可见翠湖物业案件（跨物业泄露） | 弹窗渲染处按 `roles[role].scope` 过滤 property |
| 3 | **medium** | M10 | BR-M10-06 areaPeriodPL 仅按 reportArea 手选过滤，reportArea='' 时 SE 可见非数据范围区域（沪/黄浦） | computed 加入 SE dataRange.areas 自动过滤 |
| 4 | **medium** | M1 | BR-M1-12 负责人账号常驻：成员"停用"按钮对负责人无守卫，理论可停用负责人 | 停用按钮加 `v-if="!m.isOwner"` 或拦截提示 |
| 5 | **medium** | M6/SE | SE 存证证书下载/二维码核验=●▲，但 SE nav 无 evidence 入口，无法跨案件汇总/批量下载 | SE nav 补 'evidence'（evidenceVisible 的 range 过滤已就绪 L2588） |
| 6 | low | M3 | US-M3-01 派单池无"按项目分组"聚合 UI（扁平表）；US-M3-06/BR-M3-09 释放不区分回流方向 | 增项目折叠分组；doRelease/batchReleaseSea 区分来源池 |
| 7 | low | M2/M5/M6 | 多处用户可见说明文案缺失：BR-M2-16 两类分期区分、BR-M3-19 参数仅对新案生效、BR-M4-01a 物业侧通话不重置 T_collector、BR-M6-04a 原件加密存平台 | 补 note/alert 文案 |

> 阻碍 1~5 为 medium，全部为"纵深范围过滤 / 门控守卫缺失"，非功能缺失，修复工作量小（均为单点条件补充）。修复后 Q1 可达 ≈97、Q2 可达 ≈98。

---

## 二、Q1 逐模块覆盖

### 2.1 覆盖表

| 模块 | 已实现 | 部分 | 缺失 | 覆盖率 |
|---|---|---|---|---|
| M0 总纲 | 3 | 0 | 0 | 98% |
| M1 组织与权限 | 8 | 3 | 0 | 92% |
| M2 项目批次案件主数据 | 10 | 5 | 0 | 93% |
| M3 流转·公海 | 25 | 3 | 0 | 93% |
| M4 催收作业 | 28 | 3 | 0 | 94% |
| M5 录音转写·AI话术·质检 | 10 | 2 | 0 | 95% |
| M6 存证 | 8 | 1 | 0 | 97% |
| M7 业主缴费 H5 | 3 | 0 | 0 | 98% |
| M8 结案与生命周期 | 5 | 1 | 0 | 94% |
| M9 对账结算 | 8 | 0 | 0 | 98% |
| M10 经营报表 | 3 | 1 | 0 | 95% |
| **合计** | **111** | **19** | **0** | **≈94.5%** |

> 全 11 模块 **0 缺失（missing）**，所有未达满分项均为 partial（部分实现 / 文案占位 / 范围过滤瑕疵）。

### 2.2 真实缺口清单（按 severity 排序，已对抗校验）

| severity | US/BR | 缺口 | UI 证据 | 校验结论 |
|---|---|---|---|---|
| **medium** | US-M5-06 / BR-M5-07 | 质检处置 PL/PC 无发起整改待办入口 | L993 `v-if="role==='SA'\|\|(role==='SE'&&seQcAuthorized)"`，PL/PC 走 else 显"— 只读(◐)" | **确认真实**。02§7 质检看板 PL=◐本物业，PRD BR-M5-07 三方均可生成处置待办，物业侧应能发起 |
| **medium** | US-M8-05 | maxCycleCases 未按 scope 过滤 | L2608 静态数组含 `property:'翠湖物业'`(id:103)；L1820 弹窗 `maxCycleCases.filter(x=>x.cat===grp)` 无 property 过滤 | **确认真实**。PL（阳光物业）打开弹窗可见翠湖物业案件，违反 §9 隔离 |
| **medium** | US-M10-01 / BR-M10-06 | SE 报表损益表未按 dataRange 自动过滤 | L1163 `areaPeriodPL.filter(x=>!reportArea\|\|x.area===reportArea)` 仅手选过滤；L2902 静态全平台数据 | **确认真实**。reportArea='' 时 SE 可见沪/黄浦行 |
| **medium** | BR-M1-12 | 负责人账号常驻：成员停用无守卫 | L1416 停用按钮对所有 membersVisible 行渲染，askConfirm 无 isOwner 判断 | **确认真实**。无负责人不可停用的拦截 |
| low | BR-M2-02a | 无"数据归组织、成员变动不影响可见性"用户可见说明 | 仅 L1418 alert.info 提员工子集，无 BR-M2-02a 明示 | 确认（文案占位） |
| low | US-M3-01 | 派单池无按项目分组聚合 UI | L442-489 dispatch 扁平表，无折叠分组 | 确认 |
| low | US-M3-06 / BR-M3-09 | 自动/批量释放不区分回流方向（服务商公海 vs 平台开放池） | L2809 doRelease / L2916 batchReleaseSea 均不区分来源 | 确认 |
| low | US-M3-11 / BR-M3-19 | 参数页缺"仅对新计时案件生效，存量沿用旧值"文案 | L1055-1062 settings，无该提示 | 确认 |
| low | US-M4-10 / BR-M4-01a | 物业侧通话标注 markEffective 未区分发起方（PL/PC 也提示"重置 T_collector"） | L642-643 markEffective 无角色分支 | 确认 |
| low | US-M4-11 / BR-M4-20 | 工作台待办为静态数组，无响应式优先级排序；无按案件粒度的临近释放预警 | L2731 todos 静态 | 确认 |
| low | US-M2-05 / BR-M2-06 | 批次 KPI 应收/回款率为静态演示值，未实时 computed 聚合 | L266-274 部分 computed、部分静态 | 确认（原型态可豁免） |
| low | BR-M2-04 / BR-M2-16 | T1 计时起点无时戳展示；两类分期无用户可见区分文案 | L229/L2422/L2429 | 确认（文案占位） |
| low | BR-M6-04a | 缺"原件加密存平台、举证以原件+链上哈希比对、隐私不上链"说明 | L1611-1624 证书抽屉仅展示哈希 | 确认（文案占位） |

---

## 三、Q2 角色一致性

### 3.1 6 角色一致性表

| 角色 | 分数 | perms | nav | 关键护城河校验 | 不一致项 |
|---|---|---|---|---|---|
| **SA 平台超管** | 99 | L2402 15 项全覆盖 | L2401 全 22 菜单 | 代操作 proxyFor 透明✓、redactClosed=false✓、对账只读✓ | 无实质缺陷 |
| **SE 平台员工** | 95 | L2405 仅 dispatch/assign/report.export（最小集✓） | L2405 含 platformSea/orgMgmt/settings/audit，**无 evidence** | 9 个 computed 按 dataRange 过滤✓ | ① evidence nav 缺（medium）② 报表 areaPeriodPL 未按 dataRange 过滤（medium）③ org/settings 按钮灰化非不渲染（low） |
| **PL 物业负责人** | 98 | L2408 7 项（无 evidence/legal.start/repay 写权✓） | L2407 含 reconIn 无 reconOut✓ | 对账单线隔离✓、短信充值门控✓、报表无付佣口径✓ | ① 质检无发起处置（medium，见 BR-M5-07）② 负责人停用无守卫（medium，BR-M1-12） |
| **PC 物业协调员** | 97 | L2411 12 项（法务独立主导✓、无 assign/mark/release✓） | L2410 含 reconIn 无 reconOut/members✓ | void 待派单态+本物业 scope✓、reduce 超规则转 PL✓ | 风险处置任务跟踪卡 PC 无入口（low，矩阵 PC=◐本物业理论应只读可见） |
| **VL 服务商负责人** | 97 | L2414 仅 assign/report.export | L2413 含 reconOut 无 reconIn✓ | reconInVisible provider→[]✓、evidenceVisible→[]✓、billingVisible 仅 STT/分钟✓、redactClosed=true✓、佣金区块不渲染✓ | VL nav 无 batches，§4 批次聚合 VL=○▲仅靠报表汇总覆盖（low） |
| **CO 催收员** | 97 | L2417 8 项（case.mark/suggestLegal/grab 专属✓、无 close/repay/evidence✓） | L2416 最小 4 项 | scope=provider 隔离✓、开放抢单 holdCap 阻断✓、app.html 结案脱敏✓ | case.ticket perm 与矩阵§3.1 工单 CO=○（tab 可见）文字歧义（low，UI 偏向 PRD US-M4-06 正确） |
| **OW 业主 H5** | 98 | — | h5.html 独立文件 | token 门控✓、textContent 防 XSS✓、信息最小化✓、三端物理隔离✓ | 演示 TOKENS 白名单为原型机制（落地警示，非缺陷） |

### 3.2 不一致清单（类型 / PRD 依据 / UI 现状 / severity / 建议）

| 类型 | PRD 依据 | UI 现状（实取证） | severity | 建议 |
|---|---|---|---|---|
| missing-access | BR-M5-07 三方可生成处置待办；02§7 PL=◐ | L993 仅 SA/SE 有标记/转质检/通知，PL/PC 走 else 只读 | **medium** | L993 增 `\|\|role==='PL'`，L997 处置卡增 PC 只读 |
| scope-mismatch | §9 数据隔离 | L1820 maxCycleDialog 渲染未按 scope 过滤，PL 见翠湖物业(L2608 id:103) | **medium** | 弹窗按 `roles[role].scope` 过滤 property |
| scope-mismatch | BR-M10-06 平台员工报表受数据范围约束 | L1163 areaPeriodPL 仅 reportArea 过滤，SE 见沪/黄浦 | **medium** | computed 加 SE dataRange.areas 自动过滤 |
| missing-access | §7 存证证书下载 SE=●▲ | L2405 SE nav 无 evidence；只能 L761 案件详情看单条 | **medium** | SE nav 补 'evidence'（L2588 range 过滤已就绪） |
| missing-guard | BR-M1-12 负责人不可停用 | L1416 停用对所有成员行无 isOwner 守卫 | **medium** | 停用按钮加 isOwner 判断/拦截 |
| matrix-divergence | 02§8 SE 写权需授权 | L1062/1087/1484 SE org/settings 按钮灰化+toast，非不渲染 | low | 改为 `v-if` 不渲染或保留（含解释 toast，可接受） |
| matrix-wording | US-M4-06/BR-M4-17 CO 转工单为核心动作 | L2417 CO 有 case.ticket（可创建）；02§3.1 工单 CO=○（仅 tab 可见） | low | 矩阵补 CO 建工单=●，消除歧义（UI 正确） |
| missing-access | 02§7 质检看板 PC=◐本物业 | L997 处置任务卡 v-if 排除 PC | low | 增 PC 只读分支 |
| missing-entry | §4 批次聚合 VL=○▲ | VL nav 无 batches，仅报表覆盖 | low | VL nav 补 batches 只读，或维持现状 |

### 3.3 四条护城河专项核对结论

- **资金双线隔离**：✓ 严格。reconInVisible provider→[]（L2490）；reconOut VL 专属；billingVisible provider 仅 STT/分钟（L2586）；evidenceVisible provider→[]（L2588）；项目档案佣金区块对 VL 不渲染（L115-119）；about 页"物业↔服务商零往来"。**无 over-permission。**
- **结案脱敏（BR-M8-09）**：✓ 双端落地。VL/CO redactClosed=true，canSeeDetail（L2683）结案返 false 切统计视图（L506-517），field() 兜底脱敏；app.html `_redacted` 逻辑同。SA/SE/PL/PC=false 仍见全明细，正确。
- **SE 数据范围**：基本落地（9 个 computed 过滤），但**报表 areaPeriodPL（L1163/L2902）漏过滤**（medium 阻碍 3），且 evidence nav 缺入口（medium 阻碍 5）。
- **PC/PL 权限边界**：✓ PL=结案/减免特批/作废/拨号、无 evidence/legal.start 写权；PC=法务独立主导/存证/回款冲正、无 assign/mark/release。仅质检发起处置缺口（medium 阻碍 1）。
- **CO/VL 私海公海可见性**：✓ scope=provider 隔离，CO 开放抢单 holdCap 阻断，VL 分配持仓余量条+推荐。
- **业主 H5 最小可见**：✓ 仅费用明细+收款方式，无催收/服务商信息，token 门控+textContent 防 XSS+独立文件物理隔离。

---

## 四、对抗校验修正记录

> 对所有 high/medium 与可疑 missing/over-permission 项亲自 grep/Read 复核，结果如下。

| 原审计判定 | 校验动作 | 校验结论 | 处置 |
|---|---|---|---|
| US-M8-05 maxCycleCases scope-mismatch (medium) | Read L2608 + L1820 弹窗渲染 | **属实**：L2608 含翠湖物业 id:103，L1820 filter 仅按 cat 无 property | 纳入（medium 阻碍 2） |
| BR-M10-06 SE 报表越权 (medium) | Read L1163 + grep L2902 areaPeriodPL | **属实**：filter 仅 reportArea，静态数据无 SE 裁剪 | 纳入（medium 阻碍 3） |
| BR-M5-07 PL/PC 缺处置入口 (medium/partial) | Read L993 + L997 | **属实**：L993 仅 SA/SE，PL/PC else 只读 | 纳入（medium 阻碍 1）。注：原 M5 审计标 medium、Q2 PC 审计标 low，**综合按 medium**（物业侧 PL 应能发起，影响业务闭环） |
| BR-M1-12 负责人停用无守卫 (medium) | Read L1416 | **属实**：停用按钮无 isOwner 判断 | 纳入（medium 阻碍 4） |
| SE evidence nav 缺 (medium) | Read L2405 nav + L761 案件详情存证块 | **属实**：SE nav 无 evidence，仅详情看单条 | 纳入（medium 阻碍 5） |
| doClose 写 proxyFor / auditVisible 命中 (implemented) | Read L2927 + L2583 | **属实为已实现**，非缺陷 | 维持 implemented（防误报） |
| reconInVisible/evidenceVisible/billingVisible provider 防御 (implemented) | grep+Read L2490/2586/2588 | **属实为已实现**，纵深防御正确 | 维持 implemented（防误报） |
| qc 高风险告警卡 SA/SE 门控 (implemented) | Read L985 | **属实**：v-if SA/SE，否则只读 | 维持 implemented |
| 批次发起结算 batchAllClosed 门控 (implemented) | grep L280/2754/2755 | **属实** | 维持 implemented |
| CO case.ticket 是否 over-permission | grep 矩阵 §3.1 工单行 + L2417 | **非越权**：矩阵 ○ 指 tab 可见，PRD US-M4-06 要求 CO 创建工单，UI 正确，矩阵文字需补充 | 降为 low matrix-wording（防误报为 over-permission） |
| SE org/settings 按钮"灰化非不渲染" | Read L1062/1087/1484 | **属实但 low**：灰化+解释 toast 是有意 UX，非硬越权 | 维持 low |

> **校验未推翻任何 implemented 误报，未发现漏报的 over-permission**。两路审计的 medium 判定全部经独立取证证实为真。唯一口径调整：BR-M5-07 综合取 medium（原 PC 侧审计标 low），CO 工单从疑似 over-permission 厘清为 low matrix-wording。

---

## 五、结论与后续建议

### 结论

1. **Q1 实现度 94.5、Q2 一致性 96.8**，较基线 08（92/93）稳步提升，全 11 模块 0 缺失。资金双线隔离、结案脱敏、代操作透明三条护城河经独立取证确认无 over-permission、无脱敏失效。
2. **未达 ≥98 的根因是 5 处 medium 范围过滤/门控守卫缺口**（BR-M5-07、US-M8-05、BR-M10-06、BR-M1-12、SE-evidence-nav），均为单点条件补充即可修复，非结构性缺陷。
3. 其余 14 项 partial 多为文案占位与原型态豁免（ASR/LLM/上链/定时任务/实时聚合），符合原型评估规则，不计结构性缺失。

### 后续建议（收敛轮4·优先级排序）

1. **P0（解锁 ≥98）**：修复 5 处 medium —
   - L993 质检处置增 `\|\|role==='PL'`、L997 增 PC 只读；
   - maxCycleDialog 按 `roles[role].scope` 过滤 property；
   - areaPeriodPL computed 加 SE dataRange.areas 自动过滤；
   - 成员停用按钮加 isOwner 守卫；
   - SE nav 补 'evidence'。
2. **P1（文案补全）**：BR-M2-16 两类分期、BR-M3-19 参数仅对新案生效、BR-M4-01a 物业侧不重置 T_collector、BR-M6-04a 原件加密说明、BR-M2-02a 数据归组织——5 句用户可见提示。
3. **P2（体验增强）**：派单池项目分组折叠（US-M3-01）、释放回流方向区分（BR-M3-09）、工作台待办响应式优先级排序（BR-M4-20）、VL nav 批次只读入口。
4. **矩阵维护**：在 02 矩阵明确 CO 建工单=●、SE evidence=●▲ 的 nav 含义，消除"标签可见 vs 操作权"的文字歧义。
