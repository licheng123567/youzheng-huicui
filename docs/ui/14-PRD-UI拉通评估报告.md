# 有证慧催 · PRD × 用户故事 × 三端原型 全面拉通评估报告

> 日期：2026-06-23 ｜ 范围：`docs/prd/*`（12 模块）+ `docs/ui/高保真/{index,app,h5,login}.html` + `docs/ui/02-角色操作权限矩阵.md`
> 方法：4 路并行只读审计（PRD 内部一致性 / 用户故事↔PRD / 原型↔PRD / 跨切面隔离权限），交叉核对去重。
> **初版只出报告未改动；2026-06-23 已按四批修复，下方每条标注【状态】。**
>
> **状态图例**：✅ 已修复 ｜ 🟡 部分修复（含无害遗留）｜ ⏳ 遗留（未做/待产品定调）｜ ➖ 无需改（仅记录）
>
> **修复进度总览（二轮已清遗留）**：高 H1–H4 ✅ ｜ 中 M1✅ M2✅ M3✅ M4✅ ｜ 低 L1✅ L2✅ L3✅ L4✅ L5✅ L6✅ ｜ 缺口 2a ✅、2b App✅/SE范围✅ ｜ 回灌 R1✅ R2✅ R3➖ R4✅。**全部冲突/缺口已处理**（除 R3 本属"无需改"）。
>
> **⚠ 结算模型后续再修订（2026-06-24，随 OpenAPI 契约草稿）**：结算从"按月自动增量对账 + 确认/驳回/申诉 + 案件级在线异议"改为 **「支付申请单」模型**——按案件明细**手动组单**、**收款方生成**(收佣线平台/付佣线服务商)、付款方付款、生成方**完成前可撤销/撤回重生成**、完成留凭证。**H2/2a 里的"案件级异议(BR-M9-12c)"已作废**，被"撤销重生成"取代（BR-M9-12a/b/c作废/d）。详见 `docs/api/`。

## 总体结论

近期密集迭代的功能（工作台经营化、催收员佣金人→批次→明细→支付单据、收/付佣对账、平台话术库/AI 配置/作战手册采纳闸、质检复核/处置跟踪、派单抽屉、App 通话三件套、H5 门控、统一登录、自助账号设置）在**原型与 PRD 的对齐度很高**，多数新规则已正确落地。

冲突集中在**两处迭代尾巴没扫干净**——①**M9 结算的旧"批次结清"口径**（BR + US + 总纲 CFG 三层残留，与新"明细级增量"互斥）；②**减免"协调员特批"旧表述**（3 处）。外加**一处数据层隔离洞**（对账主表用未裁剪的 `reconRollup`，防御性的 `reconView` 是死代码）。其余为编号、措辞、缺口类。

严重度统计：🔴 高 = 3 ｜ 🟡 中 = 4 ｜ 🟢 低 = 6 ｜ 缺口（缺 US/缺端）= 6 ｜ 建议回灌 PRD = 4。

---

## 一、冲突清单（按严重度）

### 🔴 高

**H1 · M9 结算口径自相矛盾：旧"批次结清一次性"未随新"明细级增量"清理** — ✅【已修复】BR-M9-15/16、US-M9-01/02/07/08、总纲 CFG-RECON-TRIGGER 全改明细级增量。
新模型 BR-M9-12a 已明确"按月增量、可跨月多次、批次结案才自动清尾，替代原批次结清一次性"，OQ-M9-1 也已重定。但下列仍是旧口径，直接打架：
- `09-结算与对账.md` **BR-M9-15**（结算时机=批次全部结案后一次性出对账单）；
- `09` **US-M9-01 / US-M9-02** 验收："批次全部案件已结案 / 批次结清结算运行"；
- `09` **US-M9-07 / US-M9-08** 触发仍写"某批次结清结算时"；
- `00-总纲.md` **CFG-RECON-TRIGGER** 默认值 = "按批次结清"，描述引用旧 BR-M9-15。
建议：BR-M9-15 废止或改写为"按月增量出账、批次结案自动清尾"；US-M9-01/02/07/08 验收改为"平台按月对未结明细增量生成 / 可勾选未结明细立即结算"；CFG-RECON-TRIGGER 默认改"明细级增量·按批次×月"。

**H2 · 收佣侧确认权口径冲突：US-M9-06 仍给物业整单确认/驳回权** — ✅【已修复】US-M9-06 改为物业只查看+平台确认收款，并补案件级异议 GWT。
`09` **US-M9-06** 验收写"对账单生成→物业确认→入账/锁定；驳回→申诉"，但 **BR-M9-12b 已收窄为"收佣侧改为平台确认收款，物业只查看"**。两者互斥。
建议：US-M9-06 改为"收佣线由平台确认收款；物业仅查看"。【已废止】单笔异议/在线异议改为支付申请单撤销/撤回重生成（BR-M9-12c 取消在线异议）。

**H3 · 减免"协调员特批"旧表述残留，与新阶梯决定权模型冲突** — ✅【已修复】00 OQ-2、07 BR-M7-06/OQ-M7-1 三处改为阶梯·决定权（无协调员特批）。
新模型 BR-M2-18/18a 的决定权只有：催收员自决 / 线下内部流程（系统仅留痕、不审批）/ 物业负责人系统内直接核准——**没有"协调员特批"这一角色或系统审批动作**。但 3 处仍写"超规则协调员特批"：
- `00-总纲.md` OQ-2；`07-业主缴费H5.md` BR-M7-06；`07` OQ-M7-1。
同一开放问题在 `02`（OQ-M2-2/6）已收敛到新模型，却与 00/07 的旧"已定"结论并存。
建议：3 处统一改为"超催收员自决档→系统仅留痕、物业内部线下处理或物业负责人系统内核准（BR-M2-18a/18）"，OQ 结论以 02 为准。

**H4 · 资金双线隔离：对账主表数据源缺范围裁剪，防御计算属性是死代码**（原型层） — ✅【已修复】`reconRollup` 接入双线硬隔离（reconIn 服务商空/reconOut 物业空）+ scopedBatches 范围裁剪 + 去硬编码"阳光花园"；`pendingSettleBatches` 同步。
对账主表实际迭代 `reconRollup`（`index.html:896`），其范围裁剪只有一句 reconIn property 分支且**硬编码 `'阳光花园'`**：付佣线（reconOut）无 provider/range 过滤、reconIn 无 range 分支。真正做了纵深防御的 `reconInVisible`/`reconOutVisible`/`reconView` **被导出但模板从未引用**（死代码）。现状不泄露仅因 nav 物理隔离（PL→reconIn、VL→reconOut），数据层无二次防御。`pendingSettleBatches`、`reverseCommission` 同样无 scope 过滤。
建议：对账主表改用 `reconView`（或令 `reconRollup` 内联 scope 逻辑），补 reconOut/range 分支、删硬编码，删除/启用死代码。
（注：此为隔离正确性问题，列高；演示时若用角色切换器直切 view 或深链会暴露。）

### 🟡 中

**M1 · BR-M1-04 与 BR-M1-04a 措辞冲突** — ✅【已修复】BR-M1-04 加注"成员账号以 04a 为准、代管理仅指业务动作不含代建"：BR-M1-04 说平台可"代物业/代服务商管理（含成员）"，BR-M1-04a 说平台"只建管平台员工、跨组织只读、不代建不停用"。建议 BR-M1-04 加注"成员账号创建/停用以 04a 为准，'代管理'仅指代操作业务动作（撤案/充值 BR-M1-15），不含代建成员"。

**M2 · `disposeTasksVisible` 给非平台 scope 留了非空数据**（原型层） — ✅【已修复】非平台 scope 一律 `return []`（数据层硬隔离）：风险处置任务跟踪应"仅平台可见、两侧不查看"（BR-M5-07b），但该计算属性的 provider/property/range 分支返回非空（property 返回 `['安信催收','恒诚催收']`）。当前安全仅因渲染位置受限。建议对非平台 scope 一律 `return []`，把隔离从渲染位置升级到数据层。

**M3 · "阳光物业"/"阳光花园"双名口径分裂**（原型层） — ✅【已修复·二轮】实时对账路径走 scopedBatches；**删除死代码** reconInVisible/reconOutVisible/reconView（含"阳光物业"字面量及 export）；projects 加 `org` 字段、scopedProjects range 按 org 物业维过滤，组织↔项目维度打通：`reconInVisible`/`qcVisible`/`evidenceVisible` 按物业名 `阳光物业` 裁剪，`scopedProjects`/`reconRollup` 按项目名 `阳光花园` 裁剪。演示混用同一字面量，正式版按归属 join 易错位。建议抽 `orgOf(proj)/scopedProjects` 统一"组织→项目→批次" join。

**M4 · US-M5-04 作战手册采纳验收过时** — ✅【已修复】US-M5-04 补 GWT：采纳人含协调员（05a）+ 分级采纳闸（05b）+ 三处提醒：只写"物业负责人审阅采纳、即生效"，未体现 BR-M5-05a（负责人或协调员均可采纳）与 BR-M5-05b（分级采纳闸：默认强制人工 / 低风险可自愿自动可回滚 / 敏感始终人工 + 三处提醒）。建议补 GWT。

### 🟢 低

**L1 · BR-M9-01a 编号重复** — ✅【已修复】"佣金基数口径"拆为 BR-M9-01b，引用（BR-M9-15/OQ-M9-6）已核对：`09` 中 BR-M9-01a 同时指"收佣比例录入时点"和"佣金基数口径"两条不同规则。建议把后者改新号（如 BR-M9-01b）并核对引用。

**L2 · 收佣线 PL「申诉/标记已处理」死代码**（原型层） — ✅【已修复】删除 `view==='reconIn'&&role==='PL'` 申诉分支，仅留付佣线 VL：`index.html:934-935` 留有 `view==='reconIn'&&role==='PL'` 的申诉按钮，与 BR-M9-12b（收佣线物业只查看）相悖；因申诉表只在 reconOut 渲染，PL 实际看不到（死代码），但应清理避免回归。

**L3 · permMatrix 汇总表 ↔ 权限文档 ↔ 页面实现 三处口径不一** — ✅【已修复·二轮】定调 BR-M5-07a「谁的员工谁处理」：VL 处置本商、PL/PC 处置本物业员工·上报跨组织、SA/SE 复核。`02-角色操作权限矩阵.md` 质检行 + 减免行（→阶梯决定权，CO 自决档）+ FW6 注全部回写，与 permMatrix/页面三方一致：质检处置（permMatrix 写 PL/PC=上报，文档 §7/§9 FW6 后写"可处置"，页面实现=上报）、收佣对账（permMatrix PL=◐，文档=●，页面 PL 可申诉）、减免（permMatrix CO=●自决，文档 §3.2 CO=—）。**文档 §7/§9 自身就有"可处置 vs 上报"矛盾**，需产品定调后三方（文档/permMatrix/页面）单点收敛。

**L4 · 佣金基数税务口径未在界面体现** — ✅【已修复·PRD】BR-M9-01b 加"展示层标注基数=减免后实收·不含税"；原型展示注释建议实现期落（纯展示）：BR-M9-01a 定"基数=减免后实收、不含税"，原型对账/佣金界面只显"基数×比例=应结"，无"不含税"标注，演示易误解。建议加纯展示注释。

**L5 · US-M8-01 结清来源措辞易误读** — ✅【已修复】US-M8-01 注明"本期回款唯一入口=协调员标注；H5 在线到账属后期"：写"H5 或线下"，但本期 H5 只展示不收款（BR-M7-03），唯一回款入口是协调员线下标注。建议注明"本期回款来源=协调员标注；H5 在线到账属后期"。

**L6 · SE 未授权 UI 行为口径不统一**（原型层） — ✅【已修复·二轮】新增 **BR-M1-04d** 定调：未授权统一为「入口可见但只读/禁用 + 需授权提示」（非不渲染）：话术库/AI 配置/新建组织/员工=入口可见但禁用+提示；案件等=不渲染。建议 PRD §8 明确"未授权 = 只读提示 还是 不渲染"，并统一原型。

---

## 二、缺口清单

### 2a. BR 有规则、无用户故事承载（建议补 US）
| BR | 缺什么 | 建议 US（actor） | 状态 |
|---|---|---|---|
| **BR-M5-13** AI 能力配置中心 | 无 US，仅 BR | 平台超管 | ✅ 新增 US-M5-07 |
| **BR-M5-12a** 话术级自我迭代 | 无 US | AI 变体/A/B/晋升闸 | ✅ US-M5-05 扩写承载 |
| **BR-M5-05b** 分级采纳闸 | US-M5-04 未承载 | 见 M4 | ✅ US-M5-04 补 GWT |
| **BR-M9-12c** 案件级回款异议（【已废止】改为支付申请单撤销/撤回重生成） | 无 US | 单笔异议→平台核对 | ✅ US-M9-06 补 GWT（已废止，纠错走撤销重生成） |
| **BR-M9-19** 服务商设佣金/标支付 | 服务商 actor 无 US | 服务商负责人 | ✅ 新增 US-M9-10 |

### 2b. PRD 要求但原型缺失
| 缺失 | 涉及 | 说明 | 状态 |
|---|---|---|---|
| **App 端催收员「我的结算/佣金」自查** | US-M9-09 / BR-M9-19a | PC 已有，App 缺 | ✅ app.html「我的」已加只读自查 |
| **SE 数据范围三维（服务商/物业维）在对账/计费链路** | BR-M1-14 | range 仅按区域过滤 | ✅【二轮】projects 加 `org`、scopedProjects range 加物业(org)维过滤；对账链路经 scopedBatches 继承；服务商维由 reconOut/批次链路承载 |

---

## 三、原型已做、建议回灌 PRD
| # | 原型已做 | 建议 | 状态 |
|---|---|---|---|
| R1 | **App 分机型自动录音引导 + 录音目录可改/重检测救济** | 回灌 BR-M4-01b | ✅ 新增 BR-M4-01c 承载 |
| R2 | **权限矩阵 14×6 总览表**（参数配置内可视化+导出） | M1 增"提供权限矩阵总览（可导出）" | ✅【二轮】新增 BR-M1-04c |
| R3 | 案件级异议**红冲/补差**展示口径 | 无需改 PRD，后端对齐 | ➖ 无需改（已记录） |
| R4 | SE 未授权细颗粒只读态 | 同 L6，§8 补充并统一 | ✅【二轮】BR-M1-04d 承载 |

---

## 四、自洽性确认（已核查无冲突，仅记录）
- **坏账/撤案**：填原因留痕、不设审核流（M8/00/01/02 一致，无残留审批）。
- **AI 群**：BR-M5-04/04a/05/05a/05b/06/06a/12/12a/13 内部自洽，与 BR-M1-04b 对齐；通话中实时提示恒关（OQ-M5-1）。
- **通话/录音**：BR-M4-01a/01b、OQ-3、OQ-M4-2 口径一致（不主动拨号+本机系统录音+获取最新+Web 救济）。
- **短信签名/模板**：BR-M4-16 / BR-M9-09 / 00 一致（平台按项目配置、物业不创建无审核）。
- **缴费主链 M4→M7→M8**：US-M4-05→US-M7-01/02→US-M4-08→US-M8-01 首尾衔接、责任人一致，无断点。
- **结案脱敏（BR-M8-09/09a）**：三端一致（列表 `field()` 兜底 + 详情 `canSeeDetail()` 收敛 + `redactClosed` 仅 VL/CO；本人业绩例外亦实现）。
- **防倒挂**（催收员比例 ≤ 付佣比例）、**登录统一入口**（账号/验证码、无入口分类）、**报表导出留痕**（`exportList→logOp`，BR-M10-08）均已落地。

---

## 五、修复执行情况（四批已于 2026-06-23 执行）

**第一批 口径一致性·PRD — ✅ 完成**：H1 M9 结算口径统一（BR-M9-15/16、US-M9-01/02/07/08、CFG-RECON-TRIGGER）；H2 US-M9-06 收佣改平台确认+案件级异议；H3 减免"协调员特批"3 处清理；附 M1 加注、L1 重号、L4 税口径。

**第二批 隔离正确性·原型 — ✅ 完成（M3 部分）**：H4 `reconRollup` 接双线硬隔离+scopedBatches 范围+去硬编码、`pendingSettleBatches` 同步；M2 `disposeTasksVisible` 非平台返回空；L2 删收佣线 PL 申诉死代码。M3 实时路径已统一口径，`orgOf()` 全局映射与死代码清理遗留。

**第三批 缺口补全 — ✅ 完成（SE 范围维 部分）**：2a 补 US-M5-05扩/US-M5-07/US-M9-10/US-M5-04（M5-12a/13/05b、M9-19、M9-12c 全部承载）；2b App 催收员「我的结算」已加。SE 范围三维（服务商/物业维）待补。

**第四批 文档收敛 — ✅ 完成（doc02/R2/R4 遗留）**：新增 BR-M4-01c（App 录音引导）；L5 US-M8-01 措辞；L3 permMatrix 质检厘清。遗留：`02-角色操作权限矩阵.md` 旧矩阵逐格回写、R2 权限矩阵总览回灌、L6/R4 SE 未授权 UI 统一。

**遗留清理（二轮 2026-06-23）— ✅ 全部完成**：
- **M3**：删除死代码 reconInVisible/reconOutVisible/reconView（含 export）；projects 加 `org` 字段、scopedProjects range 加物业(org)维。
- **L3**：定调 BR-M5-07a 处置归属；`02-角色操作权限矩阵.md` 质检行/减免行/FW6 注回写，三方一致。
- **L6/R4**：新增 **BR-M1-04d**（未授权=可见只读+提示，非不渲染）。
- **R2**：新增 **BR-M1-04c**（参数配置提供权限矩阵总览·可导出）。
- **2b**：SE 物业(org)维过滤补齐，服务商维经 reconOut/批次链路承载。

> 说明：修复同时改动 PRD（09/00/07/01/05/08/04）与原型（index.html/app.html/login.html）+ 设计文档（02-角色操作权限矩阵.md），均经 node 语法 + HTTP 200 + 无头渲染验证。**至此报告 14 全部冲突/缺口已闭环（R3 本属无需改）。**
