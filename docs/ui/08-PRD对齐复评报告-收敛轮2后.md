# 有证慧催 · 08 PRD对齐复评报告（收敛轮2 FW3+FW4 后）

> 评审综合者（opus）。基于三组覆盖审计（Q1）+ 两组角色一致性审计（Q2），经**对抗校验**（亲自 grep/Read 高保真原型逐条复核 high/medium 与可疑 missing/over-permission 项，防误报防漏报），修正后综合成本报告。
> 取证文件：`/Users/shuo/AI/cuishou/docs/ui/高保真/index.html`(2862行)、`app.html`(309行)、`h5.html`(445行)、`login.html`(228行)；口径基准：`/Users/shuo/AI/cuishou/docs/ui/02-角色操作权限矩阵.md`。
> 评审日期：2026-06-19。

---

## ① 执行摘要

收敛轮2（FW3+FW4）在轮1基础上完成 SE 导航重排（补 platformSea/orgMgmt/settings/audit、移除 evidence）、对账确认/驳回写控件改 PL(reconIn)/VL(reconOut) 专属、evidenceVisible 补 provider 防御、doClose 写 auditLog+改 status、CFG 枚举进 cfgs、工单枚举对齐 BR-M4-17、开放抢单防倒挂、案件级作废+PC▲、多号码拨打、有效/未接通跟进差异+分期暂缓释放、单案 T2 预警、报表区域周期下钻、作战手册版本 diff、质检整改待办闭环等。资金双线物理隔离（reconInVisible/billingVisible/evidenceVisible provider→空或仅分钟行）、结案脱敏（canSeeDetail+redactClosed）、SE 数据范围过滤（scoped* 系列）均经原文复核确认到位。

### 总体评分

| 维度 | 06 收敛前 | 07 收敛轮1后 | 08 收敛轮2后（本轮） | 目标 |
|---|---|---|---|---|
| **Q1 总体实现度** | 75 | 88 | **92** | 98 |
| **Q2 总体角色一致性** | 86 | 90 | **93** | 98 |

> Q1 = 各模块覆盖率加权（M0~M10 共 11 模块）。Q2 = 6 角色一致性均分（SA93/SE95/PL94/PC92/VL91/CO90/OW96，按 PC 端 6 角色取均，OW 作为 H5 单列参考）。

### 收敛轮2提升点（对抗校验确认有效）

1. **SE 导航与权限收敛**：SE nav 补齐 platformSea/orgMgmt/settings/audit 且移除 evidence 独立菜单（L2307），perms 严格最小化为 `['case.dispatch','case.assign','report.export']`（L2307），settings/orgMgmt 写按钮 SE 软禁用——与矩阵 §8 `●▲(若授权)` 默认不授权一致。
2. **对账写控件双线归位**：reconIn 确认/驳回仅 PL、reconOut 仅 VL（L828-829、L847-848），物理隔离正确。
3. **evidenceVisible provider 防御**（L2486 `if(sc==='provider') return []`）+ reconInVisible provider 返回空（L2391）+ billingVisible provider 仅 STT/分钟行（L2484）——资金双线 + 存证隔离三道防线齐备。
4. **结案脱敏收敛**：canSeeDetail（L2574）对 redactClosed 角色（VL/CO）在结案态整页切结案统计视图（L505-516），caseOps 随之清空（L2609）。
5. **CFG 枚举入库**：CFG-CLOSE-REASONS/MARK-CODES/MIN-WARN/MIN-STOP/EVIDENCE 已进 cfgs（L2393），结案受控原因下拉 + 其他必填备注（L2036-2051）。
6. **质检整改闭环**：disposeTasks + genRectify/genTraining + logOp 留痕（L2663），整改跟踪卡片（L997）。

### 未达 98 的阻碍清单（按真实 severity）

| # | 阻碍项 | severity | US/BR | 性质 |
|---|---|---|---|---|
| A | doClose 调 logOp 未传 proxyFor，SA 代物业结案审计日志无「代谁」标注 | **medium** | US-M8-03 / BR-M8-04 / BR-M1-15 | 真实代码缺陷 |
| B | qc 高风险告警卡片区（L976-986）标记/转质检/通知按钮无 role 门控，VL 进 qc 可点 | **medium** | US-M5-06 / 矩阵§7 VL=◐ | 真实 over-permission |
| C | 批次结清触发出单（BR-M9-15）无操作入口，两条对账线均静态演示 | **medium** | US-M9-01/02 / BR-M9-15 | 流程控件缺失 |
| D | 结算单据缺「发送对方对账」「打印」操作 | **medium** | US-M9-08 / BR-M9-16 | 功能缺失 |
| E | evidence/billing 失败行「重试」按钮无 role/perm 门控，PL 可触发存证重试 | low | 矩阵§3.2 存证 PL=○ | 真实权限溢出 |
| F | 6 个 CFG（COMM-IN/OUT/RECON-TRIGGER/LEGAL-FEE/SMS-COUNT/SMS-WARN）未进 cfgs | low | M9 配置项 | 文档遗漏 |
| G | PC 快捷条「建工单」绑 case.mark（CO 专属），PC 无法从快捷条建工单 | low | BR-M4-17 | 语义偏差（工单 tab 仍可达） |
| H | US-M5-02 批量补解析无「余额耗尽剩余留待解析+继续充值」文案 | low | BR-M5-02 | 文案细节 |
| I | US-M6-04 第三方/法院无登录独立核验仅 QR 占位，无核验交互页 | low | BR-M6-04 | 演示局限 |
| J | M0 §2 商业模式资金流图 / §7 全局术语表无专属展示页 | low | M0 | 文档型缺口 |

> medium 项（A~D）是拉齐到 98 的主要瓶颈：均为「流程触发/代操作留痕/对外动作」类，属后端联动边界，原型层补 4 个控件即可收口。

---

## ② Q1 逐模块覆盖表 + 缺口清单

### 模块覆盖表

| 模块 | 已实现 | 部分 | 缺失 | 覆盖率 | 复核结论 |
|---|---|---|---|---|---|
| M0 总纲 | 9 | 1 | 1 | 82% | 维持（NFR/术语属文档型，可接受） |
| M1 组织权限 | 8 | 2 | 0 | 91% | 维持 |
| M2 主数据 | 9 | 2 | 0 | 90% | 维持 |
| M3 案件流转与公海 | 10 | 2 | 0 | 94% | US-M3-06 由 medium 降 low |
| M4 催收作业 | 9 | 3 | 0 | 91% | 维持 |
| M5 录音AI质检 | 8 | 2 | 0 | **90%**↑ | US-M5-06 由 medium 降 low（PL=◐ 合规），覆盖率 88→90 |
| M6 存证 | 6 | 1 | 0 | 92% | 维持 |
| M7 业主缴费H5 | 3 | 0 | 0 | 100% | 维持 |
| M8 结案与生命周期 | 5 | 1 | 0 | 92% | 维持（US-M8-03 medium 确认） |
| M9 结算与对账 | 5 | 3 | 0 | 88% | 维持 |
| M10 经营报表 | 3 | 1 | 0 | 95% | 维持 |

**Q1 加权总体实现度 ≈ 92**（较 07 的 88 提升 +4，M5 因对抗校验上修）。

### 真实缺口清单（按 severity 排序）

| severity | US/BR | 缺口 | UI 证据 |
|---|---|---|---|
| **medium** | US-M8-03 / BR-M8-04 | SA 代物业结案时审计日志 proxyFor 恒空，被代物业看不到「代谁结案」 | index.html L2802 `doClose` 调 `logOp('结案-'+newStatus,...)` 仅传 2 参；logOp L2670 仅 `scope==='all'&&proxyFor` 才写 proxyFor，但 doClose 未传第 3 参 → SA 结案时 proxyFor=''。修复：openCloseDialog 增 proxyOrg，doClose 内 `logOp(..., targetOrg)` |
| **medium** | US-M5-06 / 矩阵§7 | qc 高风险告警卡片区「标记/转质检/通知」对全角色无门控，VL 进 qc 可点（VL nav 含 qc） | index.html L983-985 三按钮无 role 过滤；对照 L993 表格处置列已正确加 `role==='SA'||role==='SE'`。VL 矩阵=◐ 仅脱敏可见。修复：告警卡片按钮包 `v-if="role==='SA'||role==='SE'"` |
| **medium** | US-M9-01/02 / BR-M9-15 | 批次全部案件结案→触发出单，无操作控件，两线对账为静态数据 | index.html L117/L822 仅静态文案「按批次结清出单」，全文无「发起结算/触发出单」按钮。修复：批次详情页加「发起结算」（全部结案后解锁） |
| **medium** | US-M9-08 / BR-M9-16 | 结算单据缺「发送对方对账」「打印」 | openSettle L2642 构造抽屉数据，仅有下载/上传凭证占位；无发送对方/打印 |
| low | M5 / BR-M5-07 | VL 整改回填无独立入口（整改跟踪卡 L997 排除 VL），现由 SA/SE 代填 | index.html L997 `v-if="role==='SA'||role==='SE'||role==='PL'"`，VL/CO 无；PRD 说「服务商整改后回填状态」 |
| low | M9 | 6 个 CFG 未进 cfgs：COMM-IN/COMM-OUT/RECON-TRIGGER/LEGAL-FEE/SMS-COUNT/SMS-WARN | index.html L2393 cfgs 列表无上述 6 项 |
| low | US-M5-02 / BR-M5-02 | 批量补解析缺「解析到余额扣完、剩余留待解析并提示继续充值」文案 | billing 视图 L909-919 toast 仅「约 3 条/11 分钟」，无余额耗尽队列反馈 |
| low | US-M6-04 / BR-M6-04 | 第三方/法院无登录独立核验仅 QR 占位，无核验按钮交互/独立核验页 | index.html L1571-1572 QR 占位框；无校验上链一致性的操作 |
| low | US-M2-05 / BR-M2-06 | 项目列表缺回款额字段；聚合值为静态演示 | 项目列表 L93 仅批次数/在催/回款率 |
| low | US-M4-05 / BR-M4-15 | 发缴费链接无「短信 vs 复制/转微信」渠道分支弹窗 | index.html L785 发缴费码直接 openDrawer，无渠道选择 |
| low | M0 §2/§7 | 商业模式资金流图、全局术语表无专属展示页 | 散落各视图 note |

---

## ③ Q2 六角色 × 一致性表 + 不一致清单

### 角色一致性总表

| 角色 | 评分 | 资金双线隔离 | 结案脱敏 | 数据范围/归属 | 权限边界 | 主要不一致 |
|---|---|---|---|---|---|---|
| **SA** 平台超管 | 93 | ✓ 全可见 | redactClosed=false ✓ | scope=all ✓ | 全套写权限 ✓ | 代结案 proxyFor 未传（A，medium） |
| **SE** 平台员工 | 95 | ✓ ▲范围 | false ✓ | scoped* 系列全过滤 ✓ | perms 最小化 ✓ | qc 处置未叠加功能权限子集校验（low） |
| **PL** 物业负责人 | 94 | ✓ 仅 reconIn | false ✓ | property ✓ | proj.edit/close/void ✓ | evidence 重试无门控（E，low） |
| **PC** 物业协调员 | 92 | ✓ 仅 reconIn(▲只读) | false ✓ | property ✓ | 法务/存证/回款核心 ✓ | 建工单绑 case.mark（G，low）；reports 无入口（待PRD澄清） |
| **VL** 服务商负责人 | 91 | ✓ 仅 reconOut，evidence/reconIn 空 | redactClosed=true ✓ | provider ✓ | 仅 case.assign/report.export ✓ | qc 告警卡片按钮 over-permission（B，medium） |
| **CO** 催收员 | 90 | ✓ 无财务菜单 | redactClosed=true ✓ | provider 私海+公海 ✓ | 标注/建议法务/抢单 CO 独占 ✓ | qc 告警同 B 但 CO 无 qc 入口→不可达（low） |
| **OW** 业主H5 | 96 | — | — | token 门控无登录 ✓ | 隐私最小化 ✓ | 作废→token 失效跨文件未联动（low，演示局限） |

### 关键隔离专项核对（对抗校验确认）

- **资金双线隔离**：reconInVisible provider→`[]`（L2391）；billingVisible provider→仅 `/STT|分钟/` 行（L2484）；reconOut 仅 VL 写（L828）；reconIn 仅 PL 写（L828）；VL nav 无 reconIn、PL/PC nav 无 reconOut、物业 nav 无 coCommission——**双线物理隔离成立**。
- **结案脱敏**：canSeeDetail（L2574）= `!(已结清/撤案/坏账 && redactClosed)`；VL/CO redactClosed=true，结案后整页切 P-CLOSE-02 统计视图（L505-516），caseOps 清空（L2609），field() 列表态兜底脱敏（L2576）——**BR-M8-09 成立**。
- **SE 数据范围**：scopedProjects/scopedBatches/qcVisible/evidenceVisible/reconInVisible/auditVisible/membersVisible 均对 `scope==='range'` 按 dataRange 过滤（L2391/2480/2486/2481 等）——**BR-M1-14 成立**。
- **PC/PL 权限边界**：PL perms 无 case.assign/evidence/legal.start/repay/reverse/link（L2310，法务存证只读、不拨佣、不登记回款）；PC perms 含完整法务回款链 + case.void（受 voidScopeOk 本物业约束）——**与矩阵 §3.2 精确匹配**。
- **CO/VL 私海公海可见性**：visibleCases provider→本商案件（L2580），myCases 进一步 `owner==='王五'&&status==='进行中'`（L2582）；抢单 can('case.grab')+holdCap 校验（L417/L2679）；开放抢单 seaTab='open'——**BR-M1-05/M3-04/06 成立**。
- **业主 H5 最小可见**：h5.html token 门控 IIFE，valid/expired/invalid 三分支，textContent 防 XSS，DOM 无催收过程/服务商/其他案件字段——**BR-M7-07 成立**。

### 不一致清单（类型 / PRD依据 / UI现状 / severity / 建议）

| 类型 | PRD依据 | UI现状 | severity | 建议 |
|---|---|---|---|---|
| missing-proxy | BR-M8-04/M1-15 | doClose（L2802）调 logOp 不传 proxyFor，SA 代结案 proxyFor='' | medium | doClose 内 logOp 第3参传 targetOrg |
| over-permission | 矩阵§7 VL=◐ | qc 告警卡片按钮（L983-985）无 role 门控，VL 可点标记/转质检/通知 | medium | 告警按钮包 `v-if="role==='SA'||role==='SE'"` |
| over-permission | 矩阵§3.2 存证 PL=○ | evidence 失败行重试按钮（L1022）无门控，PL 可触发存证重试 | low | 重试按钮加 `v-if="can('case.evidence')"` |
| over-permission | 矩阵§6 计费 VL=○ | billing 失败行重试按钮（L927）无门控（VL 仅见 STT 行，影响小） | low | 同上加 perm 门控 |
| semantic-bind | BR-M4-17 | 建工单（L787）绑 case.mark（CO 专属），PC 无法从快捷条建工单 | low | 改绑独立工单权限或 `can('case.link')` |
| scope-mismatch | BR-M1-14 | SE qc 处置（L993）`role==='SA'||role==='SE'` 直授，未叠加功能权限子集（数据行已过滤） | low | 叠加 SE 功能授权检查 |
| scope-hardcode | BR-M1-05 | VL scopedProjects（L2495）硬编码 `p.name==='阳光花园'`，演示态非派单关联逻辑 | low | 标注：正式按派给本商批次关联项目过滤 |
| missing-access | 矩阵未明确 | PC nav 无 reports 入口（L2312） | low | 待 PRD 澄清 PC 报表阅读权 |

---

## ④ 校验修正记录（被对抗校验推翻/修正的项）

| 原审计结论 | 原 severity | 复核证据 | 修正后 | 说明 |
|---|---|---|---|---|
| **SA 对账确认/驳回缺失**（Q2 SA） | medium | 矩阵§6 L113「对账单 确认/驳回/申诉」**SA=○（只读）**，非 ●；L113 仅 PL/VL 为 ●本物业/本商 | **降为 low（部分推翻）** | 原审计误读矩阵——SA 在确认/驳回行是 ○ 只读，UI L828 SA 只读**符合矩阵**。仅 BR-M1-15 代操作语境下「SA 可代确认」为可选增强，属 low。SA 仍计 93（保留代结案 proxyFor medium） |
| **US-M5-06 PL 无质检处置**（Q1 M5） | medium | 矩阵§7 L128「风险/质检看板」**PL=◐（脱敏可见，明细不可见）**，◐ 不含写动作 | **降为 low** | PL 在 L993 为只读「◐」**符合矩阵**；原审计据「BR-M5-07 物业可处置」推断 PL 应有写按钮，但矩阵 UI 口径 PL=◐。真实残差仅 VL 整改回填入口（low）。M5 覆盖率 88→90 |
| **US-M3-06 私海 TC 倒计时缺失**（Q1 M3） | medium | TC 为后端定时任务（CFG-TC=7d，L2393）；CO 待办含「临近自动释放」（L2622）；矩阵未要求私海列展示 TC 剩余天数 | **降为 low** | 前端仅需展示状态+预警，已通过 todos 满足；TC 倒计时列非矩阵强制项。M3 覆盖率 94% 维持 |
| qc 告警 over-permission 对 **CO** | medium | CO nav（L2318）= `['workbench','myCases','providerSea']`，无 qc，onRole 强制跳回 workbench | **降为 low（确认不可达）** | CO 实际无法导航至 qc，告警按钮不可触达；代码层仍缺白名单但风险极低 |
| **doClose proxyFor 缺失**（Q1 US-M8-03） | medium | L2802 确认仅传 2 参，logOp L2670 需 `scope==='all'&&proxyFor` | **维持 medium** | 真实缺陷，对抗校验确认 |
| **qc 告警 over-permission 对 VL**（Q2 VL） | medium | L983-985 无门控 + VL nav 含 qc（L2315）→ 可达可点 | **维持 medium** | 真实 over-permission，确认 |
| **US-M9-01/02 触发出单 / US-M9-08 发送对方·打印** | medium | 全文 grep 无对应控件 | **维持 medium** | 真实缺失，确认 |
| **PL evidence 重试无门控**（Q2 PL） | low | L1022 重试按钮无 role/perm，PL 有 evidence nav 无 case.evidence | **维持 low** | 真实权限溢出，确认 |

> 修正净效应：3 项 medium 下修为 low（SA 对账、US-M5-06、US-M3-06）+ CO 告警确认不可达，使 Q1 由审计原始口径上修、Q2 SA/PL 评分微升。4 项 medium（A~D）经复核确认为真实瓶颈，纳入阻碍清单。

---

## ⑤ 结论与后续建议

**结论**：收敛轮2 显著拉齐 Q1=92 / Q2=93（较 06 基线 75/86、07 基线 88/90 稳步上升）。资金双线物理隔离、结案脱敏收敛、SE 数据范围过滤、PC/PL/VL/CO 权限边界、业主 H5 最小可见——**六大跨切面隔离规则均经原文复核成立**。距 98 目标的差距集中在 4 个 medium「流程触发/代操作留痕/对外动作」控件，非架构性缺陷，可在下一轮快速收口。

**后续建议（按优先级，预计 1 轮收敛即可达 ≈97-98）**：

1. **[medium·A] doClose 补 proxyFor**：openCloseDialog 增 proxyOrg 参数，SA 代结案时 logOp 第 3 参传被代物业名，被代方审计日志可见「代谁结案」（BR-M8-04）。
2. **[medium·B] qc 告警卡片按钮加 role 门控**：L983-985 包 `v-if="role==='SA'||role==='SE'"`，与表格处置列（L993）口径统一，VL/CO 仅 ◐ 只读。
3. **[medium·C] 批次结清触发出单控件**：批次详情页加「发起结算」按钮（全部案件结案后解锁），写 reconIn/reconOut 记录 + logOp（BR-M9-15）。
4. **[medium·D] 结算单据补「发送对方对账」「打印」**：settlement 抽屉加两按钮，发送后双方对账状态联动（BR-M9-16）。
5. **[low 批量收口]** evidence/billing 重试按钮加 perm 门控（E）；6 个 CFG 进 cfgs（F）；建工单改绑独立工单权限（G）；批量补解析补「余额耗尽+继续充值」文案（H）；存证独立核验交互（I）；VL 整改回填入口。
6. **[待澄清]** PC reports 阅读权、M0 商业模式/术语展示页——建议 PRD 侧确认是否纳入原型范围（若属文档型可保持低优先级）。

---
*报告版本：08（收敛轮2 FW3+FW4 后）｜评审方式：三组 Q1 覆盖审计 + 两组 Q2 角色审计，opus 综合者对抗校验修正｜口径基准：02 权限矩阵。*
